[package]
name = "chronicle-auto-tagger"
version = "0.1.0"
edition = "2021"
description = "Chronicle WASM plugin example: auto-tags entities on creation."

[lib]
crate-type = ["cdylib"]

[dependencies]
extism-pdk = "1.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
