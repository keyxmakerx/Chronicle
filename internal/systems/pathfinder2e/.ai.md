# Pathfinder 2e Module

## Purpose

Provides Pathfinder 2nd Edition ORC reference content. Serves spell
descriptions, creature stat blocks, equipment properties, and ancestry
features as tooltips (on @mention hover) and dedicated reference pages.

## Tier

Module (Game System -- optional, enabled per campaign)

## Domain Concepts

- **ORC Content:** Open RPG Creative content that is freely available
  under the ORC License. We can ONLY include ORC content, not proprietary.
- **Tooltip:** A small HTML fragment shown on hover when a user @mentions a
  game reference (e.g., `@spell:electric-arc` shows damage, range, traditions).
- **Reference Page:** A full page showing complete details for a spell,
  creature, item, etc.

## Files

| File | Purpose |
|------|---------|
| manifest.json | Module metadata and category definitions |
| handler.go | Serves reference pages and tooltip API |
| data/ | Static JSON files with ORC content |
| templates/ | Reference page and tooltip Templ components |

## Dependencies

- **Uses:** Nothing (read-only, self-contained)
- **Used by:** widgets/mentions (tooltip API), entity pages (reference links)

## Business Rules

- **Read-only.** This module NEVER writes to the database.
- Only ORC-licensed content. No proprietary Pathfinder content.
- Tooltip API returns HTML fragments for hover previews.
- Data files are loaded once at startup and cached in memory.

## Data Files

| File | Items | Content |
|------|-------|---------|
| `spells.json` | 20 | Cantrips through level 6 spells (Electric Arc, Fireball, Heal, etc.) |
| `creatures.json` | 14 | Level -1 to 19 (Goblin Warrior through Ancient Red Dragon) |
| `equipment.json` | 12 | Potions, runes, wondrous items (Healing Potion through Staff of Fire) |
| `ancestries.json` | 8 | Core ancestries (Human, Elf, Dwarf, Gnome, Goblin, Halfling, Orc, Leshy) |
| `classes.json` | 12 | All Player Core classes with key ability, HP, saves |
| `conditions.json` | 16 | Core conditions with mechanical effects |

## Current State

- [x] .ai.md created
- [x] manifest.json created (6 categories: spells, creatures, equipment, ancestries, classes, conditions)
- [x] ORC data files collected (82 items across 6 categories)
- [x] Status set to "available" — auto-instantiated by GenericModule loader
- [x] Routes auto-registered via GenericModule (reference pages + tooltips)
- [x] Tooltip rendering via GenericTooltipRenderer (uses manifest field definitions)
- [x] Reference pages via shared module_pages.templ templates
- [x] Searchable via ModuleSearchAdapter (Ctrl+K quick search)
