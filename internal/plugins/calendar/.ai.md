# Calendar Plugin

Two-mode calendar system for campaigns. **Fantasy mode**: arbitrary months, weekdays,
moons (with phase calculations), seasons (with wrap-around date ranges), leap years,
configurable time systems, weather state, zodiac/elemental cycles, festivals.
**Real-life mode**: Gregorian calendar synced to wall clock, user timezone-aware.
Both modes support events (single-day, multi-day, recurring with interval/end date,
with optional start/end times, color, icon, all_day). One calendar per campaign.

## Architecture

- **model.go**: Calendar, Month, Weekday, Moon, Season, Event structs + DTOs.
  `ModeFantasy`/`ModeRealLife` constants. `IsRealLife()` mode check.
  `MoonPhase()` / `MoonPhaseName()` for lunar cycle math.
  `IsLeapYear()`, `YearLengthForYear()`, `MonthDays()` for date arithmetic.
  `ContainsDate()` on Season with wrap-around support.
  `FormatCurrentTime()` on Calendar for HH:MM display.
  `HasTime()`, `FormatTime()`, `FormatEndTime()`, `FormatTimeRange()` on Event.
  `HasRichText()`, `PlainDescription()` on Event for rich text support.
- **repository.go**: SQL CRUD for calendar + sub-resources. Set* methods replace
  all records (bulk update). Event listing with entity JOIN, visibility filtering,
  recurring event support. Events sort by date then time (COALESCE for null times).
- **service.go**: One-calendar-per-campaign enforcement, validation, date advancement
  with month/year rollover (leap-year-aware), time advancement with minute→hour→day
  rollover, CRUD orchestration. Per-user visibility filtering via `filterEventsByUser()`
  and `canUserView()`. Input validation for time system values (positive integers),
  event names (max 255), event dates (month/day >= 1), visibility rules JSON.
- **handler.go**: HTTP handlers for monthly grid, timeline, settings, events, setup,
  advance date/time, import/export. HTMX detection for fragment vs full page. Mode-aware
  CreateCalendar seeds Gregorian months/weekdays for real-life mode.
- **routes.go**: Route registration with role middleware.
- **export.go**: Chronicle native JSON export format (`chronicle-calendar-v1`).
  `ChronicleExport` envelope with calendar config + optional events. `BuildExport()`
  creates export from a fully-loaded Calendar + events.
- **import.go**: Auto-detection and parsing of 4 calendar JSON formats:
  Chronicle native, Simple Calendar (Foundry VTT v1+v2), Calendaria (Foundry VTT),
  and Fantasy-Calendar.com. `DetectAndParse()` inspects top-level JSON keys to
  identify format, then delegates to format-specific parsers. Handles 0-indexed vs
  1-indexed conversion, localization key stripping, day-of-year→month+day conversion,
  and {values: {...}} nesting patterns. Returns `ImportResult` with normalized months,
  weekdays, moons, seasons, eras, and settings.
- **calendar.templ**: Monthly grid, timeline view, event modal (create/edit with
  TipTap rich text editor + @mentions, optional time picker), upcoming events fragment,
  entity-events fragment, setup page with mode chooser (Real Life / Custom Fantasy /
  Import from File). Timeline renders rich HTML descriptions with `templ.Raw()`.
- **calendar_settings.templ**: settings page tabs (General, Months, Weekdays,
  Moons, Seasons, Eras, Categories) with Alpine.js list management. General tab
  includes time system configuration (hours/day, minutes/hour, seconds/minute) and
  current time. Seasons tab includes weather effect field per season. Eras tab
  manages named time periods with year ranges. Real-life mode hides Months /
  Weekdays / Eras / Weather / Cycles / Festivals tabs and date/time/leap-year fields.
  Export button in settings header.
- **calendar_settings_wcf.templ**: Weather / Cycles / Festivals tabs (added in
  C-CAL-WCF-UI, 2026-05-19). Wire missing GET-via-eager-load + PUT pairs onto
  the existing settings page. Weather tab edits the single calendar_weather row
  (PresetID + temperature + wind + precipitation + zone + description). Cycles
  tab manages named periodic cycles + their entries (Tyr's Endlean / Seofean
  shape). Festivals tab manages fixed calendar entries (month+day OR after_month
  intercalary). All three forms render the wire-contract `{error, message,
  category}` error shape inline via respondSettingsError (see settings_errors.go).

## Data Model

- `calendars` — id, campaign_id, mode, name, description, epoch_name,
  current_year/month/day, hours_per_day, minutes_per_hour, seconds_per_minute,
  current_hour, current_minute, leap_year_every, leap_year_offset
- `calendar_months` — id, calendar_id, name, days, sort_order, is_intercalary,
  leap_year_days
- `calendar_weekdays` — id, calendar_id, name, sort_order
- `calendar_moons` — id, calendar_id, name, cycle_days, phase_offset, color
- `calendar_seasons` — id, calendar_id, name, start_month/day, end_month/day,
  description, color, weather_effect
- `calendar_eras` — id, calendar_id, name, start_year, end_year (nullable=ongoing),
  description, color, sort_order
- `calendar_events` — id, calendar_id, entity_id (FK), name, description (ProseMirror
  JSON for rich text, plain text for legacy), description_html (pre-rendered sanitized
  HTML), year/month/day, start_hour, start_minute, end_year/end_month/end_day, end_hour,
  end_minute, is_recurring, recurrence_type, visibility, visibility_rules (JSON,
  per-user allowed/denied lists, migration 000037), category, created_by

## Time System

Calendars have a configurable time system with three parameters:
- **hours_per_day** (default 24) — How many hours in one day
- **minutes_per_hour** (default 60) — How many minutes in one hour
- **seconds_per_minute** (default 60) — How many seconds in one minute

Current time is tracked via `current_hour` and `current_minute`. The `AdvanceTime()`
service method handles rollover: minutes overflow into hours, hours overflow into
days (which then roll over months/years via the same logic as `AdvanceDate()`).

Events optionally have `start_hour/start_minute` and `end_hour/end_minute` (all
nullable). When set, times display on grid chips, timeline, and entity event lists.

## Calendar Modes

Two modes controlled by `calendars.mode`:

- **Fantasy** (`mode='fantasy'`, default): Full customization — arbitrary months,
  weekdays, time units, leap years. All settings tabs visible. Setup seeds generic
  12×30-day months and 7 weekdays.
- **Real-Life** (`mode='reallife'`): Gregorian calendar synced to wall clock.
  Setup seeds correct Gregorian months (Jan 31, Feb 28 +1 leap, etc.), weekdays
  (Sunday–Saturday), 24/60/60 time, leap year every 4. Settings page hides Months,
  Weekdays tabs and date/time/time-system/leap-year fields (Gregorian is fixed).
  Current date/time derived from `time.Now()` + user's IANA timezone.

User timezone stored in `users.timezone` (nullable, default UTC). Managed via
`GET /account` settings page.

## Rich Text Event Descriptions

Event descriptions use the same dual-storage pattern as entity entries:
- **`description`**: ProseMirror JSON (from TipTap `getJSON()`) — used for re-editing
- **`description_html`**: Pre-rendered sanitized HTML (from TipTap `getHTML()` + `sanitize.HTML()`) — used for display

The event create/edit modal embeds a compact TipTap editor instance (no headings,
just inline formatting + @mentions). The editor is initialized lazily on first modal
open. On form submit, both JSON and HTML are sent to the backend.

Legacy events with plain text descriptions (created before Sprint 10) are handled
gracefully: `HasRichText()` returns false, `PlainDescription()` returns the text as-is.
When editing a legacy event, the plain text is loaded into the TipTap editor as a paragraph.

Timeline view renders `description_html` via `templ.Raw()` for rich text events, or
falls back to plain text `<p>` for legacy events. Tooltips use `PlainDescription()`
which returns empty for rich text events (ProseMirror JSON is not user-readable).

## Key Design Decisions

- **One calendar per campaign** (unlike maps' many-per-campaign). Enforced in
  service CreateCalendar().
- **Bulk sub-resource updates**: SetMonths/SetWeekdays/SetMoons/SetSeasons/SetEras
  replace all records rather than individual CRUD — simpler client-side serialization.
- **Percentage-free date system**: Month days and leap year days are integers.
  Year length calculated by summing month days + per-month leap extras.
- **Moon phases**: Continuous 0.0–1.0 value from `cycle_days` and `phase_offset`.
  Mapped to 8 named phases (New, Waxing Crescent, etc.).
- **Season wrap-around**: `ContainsDate()` handles seasons crossing year boundary
  (e.g., Winter from month 11 to month 2).
- **Event categories**: holiday, battle, quest, birthday, festival, travel, custom.
  Each has an icon in the UI.
- **Dual views**: Monthly grid (default) and timeline (chronological, year-grouped).
- **Per-user visibility**: Events support `visibility_rules` JSON column (migration
  000037) with `allowed_users` (whitelist) and `denied_users` (blacklist). Owners
  always see all events. Service-layer `filterEventsByUser()` applies rules after
  role-based filtering. UI in event modal (Owner-only) with Alpine.js member checkboxes.

## Routes

| Method | Path | Role | Handler |
|--------|------|------|---------|
| GET | /campaigns/:id/calendar | Player | Show |
| GET | /campaigns/:id/calendar/timeline | Player | ShowTimeline |
| GET | /campaigns/:id/calendar/entity-events/:eid | Player | EntityEventsFragment |
| GET | /campaigns/:id/calendar/upcoming | Player | UpcomingEventsFragment |
| POST | /campaigns/:id/calendar | Owner | CreateCalendar |
| DELETE | /campaigns/:id/calendar | Owner | DeleteCalendarAPI |
| GET | /campaigns/:id/calendar/settings | Owner | ShowSettings |
| PUT | /campaigns/:id/calendar/settings | Owner | UpdateCalendarAPI |
| PUT | /campaigns/:id/calendar/months | Owner | UpdateMonthsAPI |
| PUT | /campaigns/:id/calendar/weekdays | Owner | UpdateWeekdaysAPI |
| PUT | /campaigns/:id/calendar/moons | Owner | UpdateMoonsAPI |
| PUT | /campaigns/:id/calendar/seasons | Owner | UpdateSeasonsAPI |
| PUT | /campaigns/:id/calendar/eras | Owner | UpdateErasAPI |
| POST | /campaigns/:id/calendar/advance | Owner | AdvanceDateAPI |
| POST | /campaigns/:id/calendar/advance-time | Owner | AdvanceTimeAPI |
| GET | /campaigns/:id/calendar/export | Owner | ExportCalendarAPI |
| POST | /campaigns/:id/calendar/import | Owner | ImportCalendarAPI |
| POST | /campaigns/:id/calendar/import/preview | Owner | ImportPreviewAPI |
| POST | /campaigns/:id/calendar/import-setup | Owner | ImportFromSetupAPI |
| POST | /campaigns/:id/calendar/events | Scribe | CreateEventAPI |
| PUT | /campaigns/:id/calendar/events/:eid | Scribe | UpdateEventAPI |
| DELETE | /campaigns/:id/calendar/events/:eid | Owner | DeleteEventAPI |
| PUT | /campaigns/:id/calendar/events/:eid/visibility | Owner | UpdateEventVisibilityAPI |
| GET | /campaigns/:id/calendar/embed | Player | EmbedCalendar |

## Import/Export

Calendar supports import from 4 external formats and export in Chronicle's native format.

### Supported Import Formats

| Format | Detection | Key Mapping |
|--------|-----------|-------------|
| **Chronicle** | `"format": "chronicle-calendar-v1"` | Native round-trip |
| **Simple Calendar** (Foundry VTT) | `"calendar"` key (v1) or `"exportVersion"` + `"calendars"` (v2) | `numberOfDays`→Days, `hoursInDay`→HoursPerDay, 0→1 indexed |
| **Calendaria** (Foundry VTT) | `"days.hoursPerDay"` or months-as-object | `days`/`leapDays`, day-of-year seasons, localization key stripping |
| **Fantasy-Calendar.com** | `"static_data"` + `"dynamic_data"` | `timespans`→Months, `global_week`→Weekdays, `cycle`→CycleDays |

### Export

`GET /campaigns/:id/calendar/export?events=true` returns JSON in Chronicle's native
format with all sub-resources + optional events. Settings page has download button.

### Import Flows

1. **Setup import** (`POST /calendar/import-setup`): Upload file during calendar
   creation. Creates new calendar + applies import + auto-enables addon.
2. **Existing calendar import** (`POST /calendar/import`): Replaces all sub-resources
   on an existing calendar. Destructive operation (months, weekdays, moons, seasons,
   eras all replaced).
3. **Preview** (`POST /calendar/import/preview`): Returns parsed preview without applying.

### Sync API

Import/export also available via REST API:
- `GET /api/v1/campaigns/:id/calendar/export` (read permission)
- `POST /api/v1/campaigns/:id/calendar/import` (write permission)

## Sync API Integration

Calendar has full REST API endpoints exposed through the sync-api plugin for external
tools (Foundry VTT). See `internal/plugins/syncapi/` for those routes.

## Audit Log Taxonomy

Calendar mutations emit audit events at the HTTP handler layer (entities-plugin pattern) via `Handler.logCalendarAudit`, wired by `app/routes.go::calendarHandler.SetAuditService(...)`. Emission is fire-and-forget — failures `slog.Warn` but never block the primary write.

Event names (constants in `internal/plugins/audit/model.go`):

- `calendar.created`, `calendar.updated`, `calendar.deleted`
- `calendar.months_set`, `calendar.weekdays_set`, `calendar.moons_set`, `calendar.seasons_set`, `calendar.eras_set`, `calendar.categories_set`
- `calendar.weather_set`, `calendar.cycles_set`, `calendar.festivals_set`
- `calendar.weather_zones_set` — catalog replace (PR #360)
- `calendar.weather_active_zone_changed` — reserved for `SetActiveWeatherZone` direct calls (no current HTTP entry point; service-method only)
- `calendar.event_created`, `calendar.event_updated`, `calendar.event_deleted`, `calendar.event_visibility_changed`
- `calendar.date_advanced`, `calendar.time_advanced`
- `calendar.imported` — full import (file upload or setup-time)

**Counts-only payload discipline:** bulk Set* events log `{count: N}` in `Details`, not the full array. This matches V1-E precedent + keeps audit_log row size bounded for high-cardinality settings (e.g. categories).

**Per-era CRUD + `SetActiveWeatherZone` + `SetEventTierDefinitions`:** these service-layer methods have no HTTP entry point in calendar plugin today (`SetEventTierDefinitions` is exposed by the campaigns plugin and emits `campaign.event_tier_definitions.updated` there). Wave 1 will surface per-era CRUD via the in-card editor; audit emission will follow at that handler.

**Foundry sync (per-campaign-token) writes are NOT audited today.** SyncAPI's `APIHandler` uses per-campaign tokens with no user session; emitting with empty `user_id` would fail the audit-log validator. Operator decision required on actor model (synthetic `foundry-sync` user vs separate sync-audit table vs skip). Tracked as a follow-up.

## Security & Validation

- **IDOR protection**: `requireEventInCampaign()` verifies event's calendar belongs
  to the campaign before allowing modifications.
- **HTML sanitization**: Rich text `description_html` is sanitized via `sanitize.HTML()`
  on both Create and Update paths.
- **Input validation**: Event names (max 255), month/day (>= 1), time system values
  (hours_per_day, minutes_per_hour, seconds_per_minute must be >= 1), visibility
  enum (everyone/dm_only only), visibility_rules JSON validated on all write paths.
- **Visibility filtering**: `canUserView()` applies role-based + per-user JSON rules.
  Malformed JSON fails open with `slog.Warn` (validated on write, so only affects
  manually corrupted data). Owners always see everything.
- **Role enforcement**: Route middleware + CSRF tokens on all state-changing endpoints.

## Known Limitations

- **Recurring events**: shipped (chronicle#461, 2026-06-11) — weekly/biweekly/monthly/
  custom share the sessions recurrence vocabulary via a single `Event.OccursOn`
  expansion predicate, leap-aware monthly (migration 011). Multi-day-span recurrence
  + recurrence end-date UI controls remain future polish; still no "edit series" workflow.
- **No event duplication**: Users must manually recreate similar events.
- **No bulk delete**: Events must be deleted individually.
- **ApplyImport IS transactional**: `repository.go`'s `ApplyImport` wraps the multi-step
  import (calendar + months + weekdays + moons + seasons + eras) in one
  `BeginTx`/`Commit`/deferred `Rollback`.

## Entity Ties (C-CAL-ENTITY-TIES-DATA-MODEL, migration 009)

Optional many-to-many ties between **entities** and calendar **events/eras**,
each carrying a **participation role**. The real persistence behind Phase 1.5's
mock attach-entity picker (operator: "an entity can be but does not have to be
tied to events and eras" — both sides optional). **PR1 shipped the data model + service;
PR2 shipped the dual-side UI hooks** (create-entity ties picker in `calendar_v2.templ`
+ the `entity_calendar` block).

- **`participation_role` enum** (`entity_ties.go` `ParticipationRoles`):
  `involved · present · affected · mentioned` — PINNED to Phase 1.5 R3's
  showcase picker (`__calParticipationRoles`); keep identical so the Phase-2
  port swaps mock→real with no translation. Event ties require a role (empty →
  `involved`); era ties are coarser (role nullable). Validated in the service,
  stored as VARCHAR (not a SQL ENUM — easier to extend).
- **migration 009** (`entity_event_links`, `entity_era_links`): placement =
  **option (a)** hard cross-plugin FKs. Trivially safe: `entities` is a CORE
  table (always migrated before plugins), `calendar_events`/`calendar_eras` are
  this plugin's (migration 001). Precedent: `calendar_events.entity_id` already
  hard-FKs `entities(id)`. All FKs `ON DELETE CASCADE` → **cascade-on-delete is
  DB-enforced** (deleting an entity, event, or era drops its links) — no
  service-layer cascade.
- **entity_ties_repository.go**: link/unlink (idempotent upsert) + both-
  direction queries. `EntitiesForEvent`/`EntitiesForEra` JOIN entities +
  entity_types for display (same source the event list uses);
  `EventsForEntity`/`ErasForEntity` reuse the event/era column projections.
  `EntitiesForEvent`/`EntitiesForEra` take `role, userID` and apply
  `entityVisibilityFilter` (same helper `EntitiesForCalendar` uses, entities
  aliased `e` to match) — see "Entity-tie visibility leak fix" below
  (C-CAL-ENTITY-TIES-LEAK-FIX).
- **entity_ties_service.go**: enum validation + delegation. These methods are
  the **cross-plugin surface** the entities plugin (PR2) consumes via an
  interface — never a direct repo import (CLAUDE.md rule 8).
## World-State Model (C-CAL-WORLDSTATE-SERVER-MODEL, migration 008)

Server-side mirror of the showcase's browser-only `worldState` blob (the
synced atmosphere object driving the sky-band + hourglass). The deliverable is
**data + API, not UI** — it unblocks the Phase-2 production port and the
Phase-5b Foundry push.

- **worldstate.go**: the `WorldStateSeed` wire shape (camelCase JSON tags — a
  1:1 mirror of `static/js/cal-almanac.js` ~L464-481, CATALOG Part 8) + the
  pure `AssembleWorldStateSeed(cal, y, m, d, dayWeather, celestials, moonPhases,
  role)` builder. Moon phase/cyclePct/namedPhase computed from the real
  `Moon.MoonPhase` cycle math; named-phase vocab walk falls back to the
  showcase's short procedural names. New domain types: `DayWeather`,
  `CelestialEvent`, `MoonPhaseVocab`, `SpecialDay`.
- **worldstate_repository.go**: reads for the four migration-008 tables
  (`calendar_day_weather`, `calendar_celestial_events`, `calendar_moon_phases`,
  `calendar_special_days`) + `SetDayWeather`/`SetMoodTint` writes. All
  calendar-scoped (moon-phases join through `calendar_moons`).
- **worldstate_service.go**: `BuildWorldStateSeed` (loads + assembles) and
  `SetWorldState` (persists live mood + date/time via `UpdateCalendar`, emits
  `calendar.worldstate.changed`). `timepieceFill`/`timeControl` are ephemeral —
  never persisted; the seed emits `timeControl` with neutral defaults.
- **worldstate_handler.go** + routes: `GET/PUT /campaigns/:id/calendar/world-state`.
  GET = Player+ (GM-only celestial events filtered via `VisibilityRole`); PUT =
  Owner-only (route-level seam for the Phase-3 co-GM grant). No calId — resolves
  the active calendar (or `?calendarId=`). NB: the dispatch sketched an
  `/api/v1/` path but the GET/PUT are the session-auth production surface
  (roles + dm_only only exist on `CampaignContext`); a token-auth Foundry
  variant can layer on the same service later. **Superseded by
  C-CAL-WORLDSTATE-WIRE (below): the token-auth variant now exists.**
- **migration 008** also ALTERs `calendar_moons` (+base_design/tint/phase_source/
  size/orbit_speed — moon-library render params, read via `GetMoons`) and
  `calendars` (+mood_tint_color/intensity). The parity test
  `TestWorldStateSeed_ParityWithJSContract` is the load-bearing guard against
  server↔JS drift.
- **Footgun**: `SetMoons` (DELETE+INSERT replace) does not write the new moon
  render-prop columns, so a moon edit resets them to defaults. No UI authors
  them yet (read-only for now); revisit when a render-prop write path lands.

## World State on the Wire (C-CAL-WORLDSTATE-WIRE, 2026-07-26)

`calendar.worldstate.changed` and `calendar.weather.zones.changed` were
**publisher-side dead letters**. Both had live emitters — `SetWorldState`
(worldstate_service.go) and `SetWeatherZones` (service.go) — but
`calendarEventPublisherAdapter.PublishCalendarEvent` (`internal/app/routes.go`)
had no `case` for either name, so both hit its `default: return` and were
discarded before ever reaching the bus. Nothing logged. Every meteor, eclipse
and weather-zone edit the operator authored had been swallowed inside the
publisher since the day those emitters shipped. Traced by the Foundry
executor in Chronicle-Foundry-Module PR #82.

Three additive parts, all in this repo:

1. **Public types + adapter cases.** `ws.MsgCalendarWorldstateChanged` /
   `ws.MsgCalendarWeatherZonesChanged` (`internal/websocket/message.go`) plus
   the adapter cases. Pinned by `internal/app/routes_calendar_ws_test.go`,
   which asserts the WHOLE calendar mapping — a `default: return` is a
   silent-drop machine, so adding an emitter without a case must fail a test
   rather than a play session.

2. **Enriched payload, SPLIT BY AUDIENCE.** `WorldStateChangePayload`
   (worldstate.go) is a strict superset of the old `{date, moodTint}` shape —
   those two keep their exact paths because the Foundry module shipped
   `formatWorldstateLine` against them while the event was still a dead letter.
   Added: `events[]` (celestial detail with the STABLE
   `calendar_celestial_events` id, so a consumer dedupes a reconnect replay
   instead of duplicating a note), `moons[]`, `weather`, and an `audience`
   marker. Every nested type is reused verbatim from `WorldStateSeed`, so a
   consumer parses ONE celestial/moon/weather shape whether it arrived by push
   or by a GET refetch — the trap the weather path already fell into (flat
   `WeatherInput` on the wire vs nested `Weather` on the GET).

   **The split is the security property.** The hub gates on
   `Message.RequiresDM`, a PER-MESSAGE flag, so one message cannot be rich for
   the GM and redacted for the table. `BuildWorldStateChangePayloads` always
   returns a player-safe payload (visibility=everyone rows only) and, only when
   dm_only rows exist for the date, a second full payload. The service
   publishes that second one under the INTERNAL name
   `EventWorldStateChangedDM` (`calendar.worldstate.changed.dm`), which the
   adapter maps to the same public type with `RequiresDM` set. The suffix never
   crosses the wire — it exists so the audience can be expressed without
   widening `CalendarEventPublisher`, which four implementations mirror.

   Enrichment is **best-effort**: a failed celestial/moon/weather load degrades
   to the minimal `{date, moodTint}` broadcast rather than dropping the signal.
   The write already succeeded; a consumer that hears nothing has no way to
   learn it is stale. Pinned by `worldstate_wire_test.go`.

3. **Syncapi read path.** `GET /api/v1/campaigns/:id/calendar/world-state`
   (syncapi `calendar_api_handler.go` + `routes.go`) serves the same seed on
   the Bearer group, dm_only-gated by `resolveRole` exactly as every other
   syncapi calendar read is. See `internal/plugins/syncapi/.ai.md`.

`WorldStateEvent` gained `id` + `visibility` (additive; the parity test pins
top-level and moon keys, not event keys). `start_time`/`duration` keep their
showcase-JS names and mean start HOUR / duration in HOURS.

## Live Ambient WorldState on calendar_v2 (C-CAL-WORLDSTATE-PRODUCTION-PORT, Phase 2a)

The validated showcase engine (`static/js/cal-almanac.js`) now renders the
ambient worldState (sky / sun / moons / weather / hourglass / mood) on the LIVE
`calendar_v2` surface from real campaign data — **read-only** (controls are
2b/2c; GM live-control is Phase 4). `/demo/calendar/almanac` is unchanged.

- **One engine, two surfaces.** `cal-almanac.js` is now production-aware
  (additive — the demo path is byte-identical). The `data` block detects
  `#cal-v2-worldstate` → sets `PROD`; the `world-state` block then uses the
  embedded seed verbatim (it already IS the CATALOG Part-8 worldState —
  `BuildWorldStateSeed` #401). `PROD_SKIP` (one set, near the top) lists the
  demo/interaction init blocks that must NOT run in prod (time-control,
  date-setter, the two-tier pane, demo-controls, …) so demo controls don't
  ship. `moonCyclePct` prefers the server-computed `cyclePct` in prod (the
  demo's 30-day approximation is mock-only).
- **handler_v2 `ShowV2`** builds the seed for the cursor date (dm_only
  filtered by role) and passes both the struct + its JSON into the view data
  (best-effort — a seed failure never breaks the grid).
- **`calendar_v2_worldstate.templ`** renders the band: the `#cal-v2-worldstate`
  blob + the engine binding scaffold (sky containers, passive sun, hourglass).
  Moon elements are CREATED by the engine (`applyMoonDesigns`) from the seed —
  no per-moon templ loop. Helpers in `calendar_v2_worldstate_helpers.go` are
  the read-only first-paint labels.
- **CSS:** the rendering-canvas CSS (`cal-almanac.css`) is loaded as-is and is
  EXEMPT from customization-readiness per
  `decisions/2026-06-05-rendering-canvas-css-exemption.md` (see the file
  header). Band layout uses inline layout-only styles (no color) + V2
  utility/token chrome — the exemption does not leak into chrome.
- **Visual gate:** band placement/sizing is the operator's local visual pass
  (no headless browser in CI), as flagged in the PR.

## Event⇄Entity Ties on calendar_v2 (C-CAL-WORLDSTATE-PRODUCTION-PORT, Phase 2b)

The production form of Phase 1.5's mock attach-entity picker: the calendar_v2
event editor now persists **real `entity_event_links`** (#402) with a
`participation_role`. Built on the EXISTING event drawer + event CRUD (reuse,
not rebuild).

- **API** (`entity_ties_handler.go`, routes in `routes.go`): on the
  authenticated `cg` group — `GET …/events/:eid/entities` (Player+),
  `PUT/DELETE …/events/:eid/entities/:entityId` (Scribe+). IDOR closed via
  `requireEventInCampaign`. Wires to the #402 service tie methods.
- **Cross-plugin at the API boundary, not Go:** the picker SEARCHES entities
  via the entities plugin's own `GET /entities/search` (browser-side); the
  calendar endpoints only touch the calendar service. No calendar→entities Go
  import (rule 8). Same campaign-scope property as the legacy
  `calendar_events.entity_id` (the picker only surfaces same-campaign
  entities; a hard server-side cross-campaign check would need an entities
  interface — deferred, matches existing precedent).
- **UI:** the attach-entity section in `eventV2Drawer` (chips + per-tie role
  `<select>` + search) wired in `event_grid.js` (`initEntityTies`, hooked off
  `openDrawer`). Role vocabulary comes from the Go `ParticipationRoles` enum
  via `data-ties-roles` (single source — never duplicated in JS). Active for
  SAVED events; a new event shows "save first" (a tie needs an event id).
- **Day-pane worldState peek (Phase 2b-2):** the month-grid day popover
  (`dayDetailPopoverV2`) now shows the *clicked* day's moon phase(s) + weather +
  celestial events, fetched read-only from the existing #401 GET
  `/calendar/world-state?year&month&day` (same `BuildWorldStateSeed`, dm_only
  filtered by role — no new endpoint). Wired in `calendar_v2_shell.js`
  (`renderWorldStatePeek`); the root carries `data-cal-v2-year/-month` for the
  URL. The Day *view* already shows the cursor day's ambient via the 2a band.
  This completes Phase 2's read/interaction side; the write path (date/time +
  GM verbs) is intentionally Phase 4 (standalone 2c dropped).

## Co-DM Capability (C-CAL-COGM-CAPABILITY, Phase 3 / D6)

The existing `DmGrant` is now a **co-DM capability** grant, not just secret-
viewing — **no migration, no new role tier** (reuses `DmGrantIDs` +
`cc.IsDmGranted`).

- **Capability helpers** on `campaigns.CampaignContext` (single source of
  truth): `CanControlWorldState()` and `CanAuthorDmOnly()` =
  `MemberRole >= RoleOwner || IsDmGranted`. Route calendar/UI checks through
  these so the grant is honored (not just `VisibilityRole`).
- **World-state PUT** (`routes.go`): widened from `RequireRole(Owner)` to
  `RequireCapability((*CampaignContext).CanControlWorldState, …)` — the #401
  seam, now usable by a co-DM (this is the Phase-4 GM panel's authority).
- **dm_only authoring** (`handler.go` Create/UpdateEventAPI): the non-Owner
  downgrade now allows `CanAuthorDmOnly()` — a co-DM can mark a world-event
  secret. Server-side gate protects BOTH V1 and V2 surfaces.
- **UI-lie fix**: the "DM Only" authoring affordance renders only when
  `CanAuthorDmOnly`. Originally added on the V1 `eventModal` (deleted in
  C-CAL-V1-SUNSET). The gate now lives in the V2 editor drawer
  (`calendar_v2.templ`, `data.CanAuthorDmOnly`), pinned both ways by
  `calendar_v2_editor_drawer_test.go:TestEditorDrawer_VisibilityGate`. The
  `CanAuthorDmOnly` field remains on `CalendarViewData` (embed) and
  `CalendarV2ViewData`. Server-side gate (Create/UpdateEventAPI) protects both.
- **Relabel:** the grant UI is now "Co-DM Access" (was "DM Privileges" / "DM
  View" badge → "Co-DM"), making the broadened powers explicit.
- **Scope boundary:** co-DM ≠ Owner — NO ownership transfer / delete / member
  mgmt / structural calendar editing (months/moons/seasons/eras stay Owner).

## GM Live-Control Console (C-CAL-WORLDSTATE-GM-LIVE-CONTROL-PANEL, Phase 4;
## re-homed as the skypane pull-out card by C-CAL-SKYPANE-DETACH §B)

A collapsible GM console on `calendar_v2` that drives the LIVE world-state,
persisted server-side so players see it on refresh (D2 — no WS push this phase).
The production graduation of the showcase `demo-controls`.

- **Home (C-CAL-SKYPANE-DETACH §B):** the console is a **pull-out card on the
  sky strip's (skypane's) top edge**, NOT the old in-band overlay. A GM-gated
  **tab** (`.gm-console__pill`, `data-gm-panel-open`) rides the strip's top edge;
  opening it slides the dark-glass **card** (`.gm-console__card`) OUT and UP,
  OVER the command bar above, with zero reflow (absolutely positioned inside the
  `data-cal-skypane` wrapper, which takes NO z-index so the sticky header still
  covers the strip on scroll; the card opts above via its own z-index). The
  control bar is pinned at the strip edge (column-reverse) with the active sheet
  unfolding above it. **✕ / Esc / tap-outside** dismiss (Esc + tap-outside are
  document-level handlers in `gm_panel.js`, added once and re-pointed at the live
  panel on each wire). Reduced-motion safe. The always-on worldState band is gone
  from the page (see the band-detach note below); the console's markup + state
  machine + PUT wire are otherwise UNCHANGED.
- **Authority (server-side):** rendered ONLY when `data.CanControlWorldState`
  (Owner or co-DM, #406) — players/Scribes never receive the markup (not a CSS
  hide). `gmControlPanelV2` is the templ; `gm_panel.js` (plugin static) wires
  it; both gated in `CalendarV2Page`.
- **Every control round-trips `PUT /calendar/world-state`** (#401,
  capability-gated #406) — the identical wire. The PUT response is the fresh seed
  → handed to `window.__calSetWorldState` so the sky (now the sky strip's skybox,
  engine loaded via `calendarV2SkyAssets`) re-renders in place (no reload; falls
  back to reload if the engine isn't ready).
- **4a controls:** `+1hr / +1day / +Long rest / Step back` (Part-6 verbs) +
  `Set time / Set date` (the 2c-deferred absolute write) + `⏸ pause`.
  - Verbs use a new **signed** `WorldStateUpdateInput.Advance{Days,Hours,
    Minutes}` → `advanceClock()` (worldstate_service.go) does full rollover in
    BOTH directions (floored minute math + day-stepping over variable month
    lengths, leap-aware). Set-time/date use the existing absolute `Time`.
  - **Pause is client-only** (engine `setPaused`) — atmosphere freeze is
    ephemeral, NOT persisted (per #401); it's the GM's own view.
- **Chrome uses V2 tokens** (card/btn/border-edge) — it's chrome, not canvas,
  so the rendering-canvas OKLCH exemption does NOT apply here.
- **4b (done):** weather override (a `WEATHER_EFFECTS`-MUST select → persists
  to `calendar_day_weather` for the current date via a new thin
  `WorldStateUpdateInput.Weather *string`) + mood-tint picker (the 8 showcase
  `MOOD_PRESETS` as swatches + a Clear → persists to `mood_tint_*`, the #401
  mood path). Both round-trip the PUT + re-render via `__calSetWorldState`.
  Preset swatches show the canvas color they apply (a color picker, not
  chrome).
- **4c (done — closes Phase 4):** trigger-world-event control (meteor-shower /
  eclipse-solar / eclipse-lunar / blood-moon on the current day → a new
  `AddCelestialEvent` repo method; thin `WorldStateUpdateInput.TriggerEvent`)
  with a dm_only toggle gated by `CanAuthorDmOnly` (resolved at the handler;
  unknown types rejected in the service). **Folds in the Phase-3 V2 follow-up:**
  the V2 event drawer's restricted-visibility editor now renders only for
  `CanAuthorDmOnly` (else a locked "Visible to everyone"; `readVisibilityEditor`
  already defaults to public when the editor is absent). Phase 4 complete — the
  production calendar is feature-complete (read + interaction + entity-ties +
  full live GM control).

## Entity-Page Calendar Embed (C-CAL-ENTITY-PAGE-EMBED, Phase 6 pulled forward)

The `entity_calendar` block — a **template-context** entity-page embed (NOT the
dashboard `calendar_preview` upcoming-events card). On an entity's page it
shows a **compact worldstate band** (reuses the 2a sky scaffold +
`cal-almanac.js` via the `#cal-v2-worldstate` seed blob; rendering-canvas CSS
exemption applies) over **this entity's linked events** (#402 `EventsForEntity`,
each with its `participation_role`), read-only.

- **`EntityCalendarBlock(svc, cc, entityID, userID)`** (`entity_calendar_block.go`)
  is the data-prep: `GetCalendar` → `BuildWorldStateSeed` (#401) → marshal seed;
  `EventsForEntity` (#402) → **dm_only filtered by viewer role** (mirrors
  `filterEventsByUser`/`canUserView`). Returns the `entityCalendarBlockView`
  templ. Graceful: no calendar → empty-but-present; no ties → "No linked
  events" (never blank).
- **Registered in `internal/app/routes.go`** as `entity_calendar`
  (`Contexts:["template"]`, `Addon:"calendar"`, `Singleton:true` — the band
  binds the engine's fixed `#cal-v2-worldstate` id), with `calendarService`
  injected via the renderer closure (the map_editor pattern).
- New type over extending `calendar_preview` (which is a dashboard card) — per
  the dispatch recommendation.
- Band richness is a **compact default** (150px sky band, no hourglass) —
  operator visual-tune (flagged).

**Convergence + polish (C-CAL-EMBED-CONVERGE-POLISH):** `entity_calendar` is now
the ONE entity-page calendar. The old per-entity `calendar` block
(`BlockCalendarEvents`) + its `/calendars/entity-events/:eid` route +
`EntityEventsFragment` handler + the always-on auto-appended events list in
`entities/show.templ` were **deleted** (unused; superseded). Stray stored
`type:"calendar"` blocks drop silently (unregistered). Embed states are
friendly, never raw: no calendar → a **"Create calendar" CTA** to
`/campaigns/:id/calendars` (the existing setup wizard); missing entity/campaign
context → `entityCalendarUnavailable` ("couldn't be loaded") instead of a raw
404.

## The Bench: the nav Calendar tab's landing (C-CALV4-BENCH-P4, 2026-07-28)

`GET /campaigns/:id/apps/calendar` renders **the Bench**. Same route, same
path — the nav target is a bare string in `layouts/app.templ`'s `addonURLMap`
(~:706), the Extensions hub carries a second one
(`campaigns/extensions_hub.go:62-64`, pinned literally by
`extensions_hub_test.go:127`), and any path change forces a
`routes_snapshot.txt` regeneration. `AppDashboard` is a thin handler that
delegates to `bench.go` (assembly) and `bench.templ` (markup), styled by the
unlayered, self-contained `static/css/calendar-bench.css`.

**The proportion rule is structural, not cosmetic.** `benchClassify` picks ONE
primary calendar (campaign default → active → first in-world → first) and AT
MOST one real-world calendar; every other calendar is a row. `.stack` is a
column at every width and no media query changes that. The signed contract
writes the rule twice, verbatim: *one primary Block, one real-world Block, two
subordinate rows, one slot. There is no width at which this becomes four
identical panels.*

**Five things worth knowing before touching this surface:**

1. **Permission is absence, and it is counted.** A player's ribbon is three
   tiles; `benchRibbon` never BUILDS Sync / Needs attention / Horizon for them,
   so there is nothing in the DOM to grey or hide. `bench_test.go` asserts both
   the markers and the tile COUNT — a presence assertion alone cannot catch a
   fourth tile appearing.
2. **The warn TREATMENT is GM-only; the ROW is not.** The signed Bench's
   Dwarven warnrow is absent from the player render because that calendar is
   not shared with players — a visibility fact settled upstream by
   `ListVisibleCalendars`, not a secret about the fault. Conflating the two
   makes a player's own misconfigured calendar vanish silently. Both roles get
   NO date element on an unresolvable calendar; only a GM gets the diagnosis
   and the setup door.
3. **NEXT UP reads `UpcomingAcrossCalendars`, never `UpcomingByCalendar`.**
   The latter filters base visibility in SQL only and never applies
   `filterEventsByUser`, so an event carrying `visibility_rules` leaks its NAME
   to a player there. `benchSortKeys` exists so the shipped `?sort=nextevent`
   control is fed from the SAME filtered index rather than reopening that path.
4. **The owner/player list split is load-bearing.** Owners get
   `ListCalendars`, players get `ListVisibleCalendars`. Unifying them reopens
   the W5a leak, even though the Bench renders one surface.
5. **The host passes the layer set; DEF stays `["moons"]`.** `benchBlockLayers`
   enables moons / eras / weeknums / ledger / shelf, per the signed bench
   renders and the DEF ruling
   (`cordinator decisions/2026-07-28-calv4-def-and-zone-chips-ruling.md` §1).
   **It now DIFFERS from `entityBlockLayers`, on purpose** (C-CALV4-BENCH-R2
   [BR2-8]): the Bench is a cockpit and depth is its job; the entity page is an
   embed and glanceability is its job. Do not re-unify them.
   The old claim that `ledger` "cannot be dropped" because the full-tier column
   arithmetic subtracts its 300px unconditionally was **measured and found
   stale** — see the entity host section below and
   `entity_ledger_geometry_test.go`. `moongraph` and `horizon` are
   deliberately omitted: wave 1 renders each as a `needs backend` chip and
   nothing else, and at std tier the extra need-zones stack against the docked
   Ledger and the Shelf for no information at all. **W-F filled both zones and
   the host set STILL DOES NOT CARRY THEM** ([LYR-7] SIGNED,
   `C-CALV4-LAYERS-P9`): the booking wanted REACHABILITY and the switchboard
   supplies it directly, L29 makes the illumination graph's default OFF, and
   `horizon` is still chipped so seeding it would ship a `needs backend` chip
   into a default view. The set is now a SEED rather than a verdict — a viewer
   with a stored layer row overrides it, and a viewer without one sees exactly
   what this file has always rendered. The host
   adds the keys then.

**What is design-ahead, and says so.** The knowledge horizon (COMMON §6.1 — no
queryable fog) and per-calendar sync linkage (COMMON §6.3 — the numerator is
DEFINED, not queried) carry the signed `needs backend` chip. **The pin in
`bench_test.go` is an ENUMERATION rather than a number**: it names every
surviving chip by site and the count follows from the list, which is what let
W-G swap two entries without the total moving. The Block-side chips it subtracts
are down to one — the Shelf's FILTERS panel, which is not a zone chip but the
honest state of one tab inside a filled zone, and which renders to a GM only.
The mockup's "RSVP 3 / 5", "9 days fogged" and hardcoded "all 11" are fabricated
numbers and are NOT ported.

**CORRECTED 2026-07-28 (C-CALV4-RSVP-P8 §2, ADR-048 §15).** This paragraph used
to open *"Session RSVP (no session entity, no RSVP table, no per-member time
zone)"*. **All three claims were false**, and two were false on the branch that
wrote them: sessions ship `scheduled_date`/`scheduled_time` (sessions migration
004), `calendar_event_rsvps` + `calendar_event_rsvp_tokens` +
`calendar_events.collect_rsvps` shipped in this plugin's OWN migration 013 —
`calendar_v2.templ` already rendered against them — and `users.timezone` has
existed since core migration 000001. A `needs backend` chip that is wrong is a
fabricated ABSENCE, and it is worse than no chip: it reads as a verified
finding. **A chip is a preflight FINDING, and a finding names the migration or
the route it checked.**

**THE BENCH RSVP PANEL (C-CALV4-RSVP-P8 Part A).** The signed panel body
(`cv4:2205-2263`) is built and filled from shipped storage: per-member
availability lanes, the anonymous density row, a derived recommended window, and
the full member table with roles, zone chips, per-member local clocks and
answers. It is NOT a restyle — preflight measured `.ovgrid`, `.lane`, `.recbr`,
`.mrow`, `.swatch` and `.slot` at ZERO occurrences across both shipped
calendar-v4 sheets, so the whole body and its stylesheet region are new.

- **The permission law is the signed contract's** (ADR-048 §16): answers, roles,
  zones and local clocks are PARTY-VISIBLE; availability LANES are owner /
  co-DM only; the density row is everyone's. `includeDetail` is decided in the
  handler from role and passed into the sessions seam, which omits the lane data
  **from the payload** — a player's `BenchAvailability` has nothing to hide.
- **No stored aggregate reaches it** (ADR-048 §19). Every number is recomputed
  from the membership-filtered roster, so a departed member's stored
  `calendar_event_rsvps` row changes nothing. `bench_rsvp_oracle_test.go` is the
  gate and that departed row is its load-bearing case.
- **The reads go through two nil-safe seams** — `BenchScheduleReader` (the
  sessions roster + week overlay) and `RSVPService` (answers) — wired by setters
  from `internal/app/routes.go`. The session it prints comes from the Bench's
  own already-viewer-filtered `UpcomingAcrossCalendars` index, so the panel adds
  NO calendar read and the W5a split stays split.
- **ONE control is genuinely unbacked, and it is GM-tier**: Propose (no
  propose-from-window write path). The other two are closed. The recommender was
  retired by WG-3, which DERIVES the window from the overlay's own per-hour free
  counts under a permanent `derived · not stored` chip with a quorum refusal
  below three members. **Nudge is LIVE as of C-CALV4-RSVP-P8B** — it posts to
  `POST /campaigns/:id/calendar/ask` (Scribe+) as a form with a hidden
  `csrf_token`, and it is the ONLY live Nudge in the product: the two session
  tiles' Nudges were RETIRED rather than lit, because two live buttons mailing
  one roster from one page is a double-send affordance. It carries three GM-tier
  states — live / cooling down (the persisted 6h campaign cooldown, stated as a
  sentence in the `.foot`) / `email not configured` (a `.badge.warn`, never
  `.badge.need`, because an unconfigured mail server is a deployment fact and
  not a build gap).
- **Zero new routes** (WG-6). The session tile's signed trio posts form-encoded
  to the EXISTING Player+ RSVP endpoint; `SetRSVPRequest` grew `form:` tags and
  the handler branches on `middleware.IsHTMX`. `routes_snapshot.txt` is
  byte-identical at 720 lines and the JSON consumers are untouched.

Signed controls with no store still render INERT — present so the geometry is
final, disabled so they are not dead controls that swallow a click, which is the
Block's own `layersInvoker` precedent — but since WG-5 they carry a VISIBLE
`.badge.need` rather than a `title` alone, and a control that carries one is
GM-tier because the chip is.

**Retained but unrouted.** `CalendarAppDashboardData` and every component in
`app_dashboard.templ` stay put; dead-code removal is a post-wave slice, and
C-CALV4-HOST-P3 still references `adaptiveCalendarWidget`. Two of them are
still LIVE on the Bench: `calendarAppDashboardEmpty` (the role-aware empty
state) and `calendarPermissionsModal` (owners). The two loader helpers that fed
only the retired detail pane went with their sole caller rather than being left
as unreachable code.

**Booked, not fixed:** `/apps/calendar` rides the authenticated group
(`routes.go:179`) while `/calendar/v2` is public-capable (`routes.go:147-149`),
so an anonymous visitor to a PUBLIC campaign who clicks the nav Calendar tab
lands on `/login`. Fixing it needs a route-group change plus a snapshot
regeneration → a wave-2 slice.

### The entity embed hosts the calendar-v4 Block (C-CALV4-HOST-P3, 2026-07-28)

The embed's month surface is now `internal/widgets/calendar_block.Block`,
projected through the P2 spine. `adaptiveCalendarWidget` / `adaptiveCalData` /
`embedGridView` are NOT deleted — `app_dashboard.templ` belongs to another
calendar-v4 slice and dead-code removal is a post-wave cleanup.

What is around the Block is unchanged and still pinned: the compact worldstate
band and its per-band seed-blob id, the "Open full calendar" link, the Scribe+
binding affordance, and this entity's linked events.

Four things worth knowing before touching this file:

1. **The degrade ladder cannot be read.** `BlockService.Block` gates
   calendar-level visibility and answers a HIDDEN calendar with the byte-
   identical `apperror.NewNotFound` a MISSING one gets (C-CALV4-SEAM-P5 stage
   9). `EntityCalendarBlock` therefore does not branch on which: both render
   the same "no calendar" rung, and
   `TestEntityCalendarBlock_HiddenCalendarIsIndistinguishableFromNone` compares
   the two renders BYTE FOR BYTE. A convenience like "you don't have access to
   this calendar" would turn every entity page into a probe for the existence
   of hidden calendars.
2. **Ties come from `EventsForEntityFiltered`, never `EventsForEntity`.** The
   loop this replaced re-implemented only the EVENT-level filter, so a tie into
   a calendar the viewer may not see still printed that event's name. The test
   stub's `EventsForEntity` now returns an error so a regression fails loudly.
3. **The layer set is the HOST's decision, and it is deliberate.** Producer DEF
   stays `["moons"]` (cordinator `decisions/2026-07-28-calv4-def-and-zone-chips-ruling.md`
   §1: "the default surface is a month with its moon phases and nothing else");
   a host that wants more PASSES A SET, which is the mechanism the ruling named.
   `entityBlockLayers()` passes **`moons, eras, weeknums`** — three keys, not
   five — and NOT `legend` / `horizon`, which the signed renders do not show.
   **The seed dropped `ledger` and `shelf` in C-CALV4-BENCH-R2 slice R2-1
   ([BR2-8] SIGNED)**, on the operator's "on the entity it scrolls": those two
   are exactly the keys that add a ZONE (a 300px docked column, a tabbed foot),
   and the three kept are all inside the month. The five-key embed in
   `mockups/renders/v4-entity-*.png` is now a NAMED DIVERGENCE, not a contract.
   Both zones stay one switchboard click away and the click persists — though
   note the store's grain is (user, campaign), so turning `ledger` back on here
   also turns it on for the Bench. Depth returns via R2-3's Block theater and
   R2-1 ships no substitute for it.
   **The Ledger's old "not cosmetic" geometry warning was STALE and was measured
   out** (`entity_ledger_geometry_test.go`, four legs): `ColWidth` / `IsNamed` /
   `IsNamedCSS` have zero non-test callers, the density flip is a
   `@container cal-cell (min-width: 84px)` query against real layout, and the
   full-tier body track is `minmax(0, 1fr) auto` so an absent Ledger collapses
   its own track. The one real consequence is the opposite of the fear: named
   columns now flip on at a NARROWER host (1198px → 898px for a ten-day week,
   exactly the dock's 300px), so the entity month is richer, not poorer.
   **`moongraph` IS in those renders and is still omitted**, measured rather
   than assumed: with it enabled the std tier (host 420px) stacks the moongraph
   needzone row, the docked Ledger and the Shelf into one another and the
   Ledger/Shelf headers visibly collide; without it they stack cleanly. Wave 1's
   moongraph zone is a `needs backend` chip, so the collision buys no
   information. **W-F filled the zone and did NOT add the key** ([LYR-7]
   SIGNED) — the switchboard is the reachability the booking wanted. The set is
   now a SEED: `resolveBlockLayers` prefers the viewer's stored row and falls
   back to these keys, so a viewer who has never opened the switchboard sees the
   host's set. (Until R2-1 that was byte-for-byte the signed entity renders; the
   seed change is precisely what the seed rule was built to make cheap, and it
   touched no widget file.)
   **[S11]'s "the re-add needs its own measurement" caveat FIRED.** Taken fresh
   with a REAL legend and a REAL three-lane graph at 420px and 358px, the
   Ledger's head drew over the Shelf's strip. Fixed per CTS-8 INSIDE the Block's
   own std geometry — the body scrolls, the month keeps every week row, no layer
   key dropped, and no host file touched.
   **RE-MEASURED AT W-B (2026-07-28, CTS-8), because that reading was taken
   against STUBS and filling the Ledger makes it taller.** With the Ledger full
   and this exact layer set, at BOTH production host widths (entity 420px, Bench
   358px), the Ledger and Shelf boxes abut without overlapping and nothing is
   clipped — but only after the `.lrows` 176px floor was scoped to full tier,
   where the Block can afford it. Written at every tier it kept its declared
   size inside a 140px zone and landed 36px on top of the Shelf. The fix is
   inside the Block's own std geometry; **no host layer key was dropped**, which
   is what CTS-8 exists to prevent. `TestProbe_StdTierFilledLedgerDoesNotCollide`
   holds both widths.
   **RE-MEASURED AGAIN AT W-E (2026-07-28, [S11]), because that reading was
   taken against a STUB SHELF and filling zone D makes it 166px where the stub
   was 40.** The collision came back: with the Shelf refusing to shrink
   (`flex: none`) the Ledger absorbed the whole std deficit, was crushed below
   its own head and tab strip, and put 10.8px of rows on top of the Shelf at
   BOTH widths. The Shelf is the zone that can give — its body already scrolls,
   while the Ledger's furniture can only overflow — so it is `flex: 0 1 auto` at
   std and `flex: none` at full, and it yields first. Again inside the Block's
   own std geometry; again **no host layer key was dropped and no pinned host
   file was opened**. `TestProbe_StdTierFilledShelfDoesNotCollideWithTheFilledLedger`
   holds both widths with the Almanac's tallest panel selected.
   **The consequence for W-F: the std tier is now TIGHTER than either earlier
   reading, so the `moongraph`/`horizon` re-add is still one line here but needs
   its own measurement rather than inheriting W-E's.**
4. **The tie toggle is live, and it is CSS.** The Block is entity-hosted
   (`Viewer.HostEntity = entityID`, `TieMode: "tied"` — the signed default), so
   the Nameplate renders the toggle with both counts from the spine's ONE
   viewer-filtered pass. Flipping it is a hidden radio pair plus `:has()` in the
   widget package; this file supplies no JavaScript and needs no route.

**The persisted widget slug stays `calendar`.** `widget_type` is an append-only
discriminator in `widget_bindings`; a `calendar_v4` slug would orphan every
existing binding (resolution finds nothing and silently falls back to the
campaign default) and add a third instance-deleted hook to `DeleteCalendar`,
which `widget_type_test.go` pins at exactly two.

**Singleton: the `entity_calendar` block registration stays `Singleton: true`**
(`internal/app/routes.go`, unchanged by this slice — that file belongs to
C-CALV4-SPINE-P2 for the wave). The reason is the ALMANAC BAND, not the Block:
`static/js/cal-almanac.js` binds only the FIRST `[data-cal-worldstate]` on a
page, so a second sky-bearing embed would render a static, never-updating band
while looking identical to the live one — dishonest in exactly the way this
surface is not allowed to be. The Block itself is safely multi-instance (its
radio group name is a function of calendar + host entity, and every size
decision is a container query), so if the band is ever removed from this
surface, the singleton constraint can be revisited with it. Do not flip it
without moving the engine's binding off "first match".

## Dashboard Integration

- `calendar_preview` block type for campaign dashboard editor
- `calendar_full` block type — full calendar grid embed with inline HTMX month navigation
- `GET /calendar/upcoming?limit=N` serves HTMX fragment with current date, season,
  and upcoming events
- `GET /calendar/embed` serves compact calendar grid fragment for dashboard embed
  (CalendarEmbedFragment with prev/next month navigation targeting #calendar-embed)
- Entity-event reverse lookup: `GET /calendar/entity-events/:eid` serves events
  linked to a specific entity (lazy-loaded on entity profile pages)

## Calendars dashboard (E1 / C-APPS-CAL-DASH-W1)

- `GET /campaigns/:id/apps/calendar` (Owner) — the Calendars management dashboard
  (`app_dashboard.{go,templ}`). Dedicated page reached from the Extensions hub's
  per-app "Open dashboard" entry. List + detail pane: `ListCalendars` on the left;
  the selected calendar's CRUD-compose actions (open/settings/setup/delete/active-
  switch — composes EXISTING routes, no new CRUD) + a read-only associations panel
  on the right. Selecting a calendar HTMX-swaps `#cal-dash-detail` (`?calId=`).
- `EntitiesForCalendar(calendarID)` (entity_ties) — distinct entities tied to any
  event/era of a calendar (joins through `calendar_events`/`calendar_eras`; the
  link tables carry no calendar_id).
- Timelines-for-calendar comes from the timeline plugin via the `TimelineLister`
  service interface (adapter wired in `internal/app/routes.go` — no repo cross-import).
- W2 (live embeds) shipped — see below. W3 (inline link/unlink + create-timeline) was
  superseded by the widget-binding framework (`.ai/todo.md` §"Widget Binding Framework");
  W4 (generalize the per-app dashboard pattern) remains a future wave.

### Calendars dashboard W2 — live "see in action" embeds (C-APPS-CAL-DASH-W2)

- The detail pane gains a "See in action" section reusing shipped surfaces:
  - LIVE worldstate band via `worldStateBandV2` (the production sky+hourglass)
    **only when the selected calendar is the campaign's ACTIVE one** (the engine
    binds the active calendar / one `#cal-v2-worldstate` per page). Non-active →
    a friendly note (no widget surgery; default nuance).
  - Engine-free month grid lazy-loaded via the existing `/calendars/:calId/embed`.
  - Per-associated-timeline `timeline-viz` widget mounts (D3 added at page level).
- **Selection is a full navigation** (list rows are plain hrefs, not HTMX detail
  swaps): `htmx.config.allowScriptTags=false` means engine/D3 scripts in a
  swapped fragment won't run, and the engine inits from its seed at page load —
  so full-load is the robust path and makes one-live-surface + teardown automatic.

## Widget-binding worldstate (C-WIDGET-BINDING-P2)
- `worldstate_widget_type.go` registers `"worldstate"` (distinct slug) — its
  bindable instance is a CALENDAR id (the hourglass is a view over a calendar's
  clock); shares `calendarInstanceBacking` with the calendar widget type.
- `DeleteCalendar` sweeps BOTH `calendar` and `worldstate` bindings (both
  reference calendar ids) via the injected `BindingCleaner`.

## Widget-binding picker (C-WIDGET-BINDING-P4a)
- `calendarInstanceBacking` now implements `ListInstances` (campaign's calendars
  → `[]InstanceRef`, fantasy → `fa-calendar-days` / reallife → `fa-clock`) and
  `CreateInstance` (creates a named calendar via `CreateCalendar`, returns its
  id). Both are SHARED by the `calendar` and `worldstate` widget types — but only
  the calendar block surfaces a picker in P4a (worldstate picker is P4b).
- `entity_calendar_block` gained a Scribe+ **"Change calendar"** affordance: a
  bound/default badge (from the threaded `source` = `Resolution.Source`) + an
  HTMX `GET …/bindings/picker` that loads the generic `widgetbindings` picker
  into an inline panel. `EntityCalendarBlock(... calendarID, source string)` —
  the `source` 6th param is "" when the framework isn't wired (treated as
  default). Players never see it; unbound entities render exactly as before.
- The picker's bind/create/unbind live in the `widgetbindings` plugin (generic),
  NOT here — calendar only declares the `WidgetType` hooks + renders the badge.

## Widget-binding re-render seam (C-WIDGET-BINDING-P4b)
- Both `calendarWidgetType` and `worldStateWidgetType` implement
  `RenderBlock(ctx, widgetbindings.BlockRenderContext)` → `EntityCalendarBlock` /
  `EntityWorldStateBlock` wrapped in `widgetbindings.BlockHost` (the stable swap
  target). The binding handler calls this to swap the block in place after a
  bind/unbind (no more full reload).
- The P4a bespoke calendar affordance is gone — both blocks now embed the shared
  `widgetbindings.BindingAffordance`. `EntityWorldStateBlock` gained `entityID` +
  `source` params; its affordance shows only for a per-entity host (entityID != ""),
  so the campaign-dashboard context stays affordance-free.
- ⚑ The `entity_calendar` band is a bare `<script src=cal-almanac.js>` engine (NOT
  a data-widget), so under `allowScriptTags=false` it does NOT re-animate on an
  HTMX swap — the swapped fragment shows the correct server-rendered static band;
  animation resumes on the next full load (same as hx-boosted nav). `entity_worldstate`
  IS a data-widget so it re-mounts cleanly on `htmx:afterSettle`.

## Worldstate render architecture (2026-06)

The C-CAL-WORLDSTATE-GM-OVERHAUL work (chronicle#442 + the #443 r2 visual-review
fixes) reshaped how the ambient worldstate is drawn and controlled. Three things
to know before touching the sky band or GM console:

1. **CSS render/chrome split.** `static/css/cal-almanac-render.css` is the
   widget's RENDER layer — the shared styling loaded by EVERY surface (/demo,
   the `calendar_v2` band, both entity embeds, the dashboard shelf). It must stay
   shell-free: a rule scoped to `.cal-almanac-shell` silently skips production,
   which is the historical "works in /demo, broken in prod" bug class.
   `static/css/cal-almanac.css` is now demo CHROME only — it must not (re)style
   the render classes. This contract is the load-bearing guard in
   `test/js/css_render_split.test.mjs` (render layer stays shell-free, chrome
   never restyles render classes, every surface links the render layer, prod
   surfaces never load the demo chrome). NB: this supersedes the older
   "rendering-canvas CSS = cal-almanac.css" note in the Phase-2a section above.

2. **Two sky canvases + layered SKY_FX.** The engine paints a BACK canvas
   (`data-cal-sky-canvas`) and a FRONT canvas (`data-cal-sky-canvas-front`) so
   atmosphere can sit both behind and in front of the sun/moons. Weather and
   celestial events are rows in a single `SKY_FX` registry, each row declaring
   `{ back: [...], front: [...] }` FX builders. They compose by stacking — the
   `WS_LAYER_ORDER` (`timeOfDay · season · celestial · weather · events ·
   moodTint · timeControl`) means an active event (e.g. a meteor shower or
   eclipse) layers ON TOP of the current weather rather than replacing it
   (`celestialFor(...)` "can stack"). Both canvases ship on every surface,
   demo included (parity-asserted in the css-render-split test).

3. **GM console = strip + full-band sheets.** The live GM controls
   (`internal/plugins/calendar/static/css/gm_panel.css` +
   `static/js/gm_panel.js`, markup in `calendar_v2_gmpanel.templ`) are a slim
   always-visible STRIP at the band's top edge (clock + quick time verbs +
   section buttons) that collapses to a "GM" pill. Each section
   (Time/Weather/Events/Mood) opens as a translucent glass SHEET covering the
   sky pane below the strip (`data-gm-sheet` / `data-gm-sheet-open` /
   `data-gm-sheet-panel`); sheets close via ×/re-tap. The whole console is
   server-gated by `data.CanControlWorldState` (Owner or co-DM, #406) — players
   and Scribes never receive the markup, it is not a CSS hide.

## Event recurrence + editor action set (C-CAL-EDITOR-EXPANSION, 2026-06-11)

- **Recurrence has ONE expansion predicate: `Event.OccursOn(cal, y, m, d)`** (`model.go`). Types `weekly|biweekly|monthly|custom` mirror the sessions plugin's vocabulary; anything else (incl. legacy `yearly`/empty) renders once at its stored date. All three day-projection helpers (`eventsForDay`, `eventsForWeekDay`, `allDayEventsForDay`) route through it — never re-implement date matching beside it. The month/range SQL only **widens the candidate set** (`OR is_recurring … IN (four types)`); placement happens in Go. The visibility filter wraps the widened set, so dm_only recurring events never reach players. `OccursOn` uses the same constant-year `absDayIndex` space as `v2WeekdayIndexFor` ON PURPOSE — weekly events must stay aligned with the grid's weekday columns; do not "fix" it to true leap-aware day counting.
- **`recurrence_day_of_week` (migration 011) is stored but unused** by expansion (base-anchored instead) — schema parity with sessions for a future alignment, not dead code to delete.
- **Cross-plugin entity creation** ("create entity from event") goes through the `EntityCreator` interface (`entity_actions.go`) + `calendarEntityCreatorAdapter` in `internal/app/routes.go` — rule-8 seam, mirrors bestiary. Calendar never imports the entities plugin.
- **World-state PUT additive field `weatherDate`** retargets the weather upsert to an arbitrary day (drawer's "set weather for this day"); absent = current date (wire-pinned). Bounds-validated against the calendar in `SetWorldState` (month/day must exist; year deliberately unbounded for fantasy eras).

## Skypane detach — band removed, sidebar desktop-detached, GM console pull-out (C-CAL-SKYPANE-DETACH §B)

The `calendar_v2` PAGE shell changed shape; no route/migration/wire changes.

- **The always-on worldState band is GONE from the page.** `CalendarV2Page` no
  longer renders `@worldStateBandV2` (nor the `data-cal-band-region` overlay
  wrapper). The sky lives ONLY in the collapsible sky strip (`calendarV2SkyStrip`,
  which already mounts the skybox widget). `worldStateBandV2` /
  `worldStateBandVariantV2` are RETAINED — the app-dashboard "see in action"
  card still uses the shelf variant (`worldStateShelfBandV2`), and the band tests
  pin the sky-only composition — but nothing mounts the band on the calendar page.
- **Engine seed + assets moved to the sky strip.** The band used to embed the
  one-per-page `#cal-v2-worldstate` seed blob and load `cal-almanac-render.css` +
  `worldstate_provider.js` + `skybox.js` + `cal-almanac.js`. With the band gone,
  `calendarV2SkyAssets` (in `calendar_v2_sky_strip.templ`, gated on `wsActive`)
  emits them from the strip — the strip's skybox + the GM console's live
  `__calSetWorldState` re-render depend on this load. Verbatim markup; only the
  home moved.
- **Desktop mini-month sidebar is DETACHED.** `mobileSidebarClasses` now returns
  `"hidden …"` for the Month view (hidden at every width — `mobileMonthAssembly`'s
  navigator is the sole mini-month there) and `"md:hidden …"` for other views
  (detached from desktop ≥768px, mobile sidebar preserved). Net: no mini-month
  sidebar on desktop for any view (matching the signed desktop render); mobile
  rendering is byte-for-byte unchanged. The signed design keeps the mini-month
  solely as the mobile Month navigator.
- **GM console → skypane pull-out card.** See the "GM Live-Control Console"
  section above. The `data-cal-skypane` wrapper (`relative`, NO z-index) holds
  `@gmControlPanelV2` + `@calendarV2SkyStrip`; the console's tab rides the strip's
  top edge and its card slides out over the command bar with zero reflow.
- **Guards:** `calendar_v2_skypane_detach_test.go` (page render: no band, no
  desktop-visible sidebar, gated console inside the skypane, engine assets on the
  strip); `test/js/gm_panel_transition.test.mjs` (pull-out CSS contract + Esc /
  tap-outside dismiss); the `mobileSidebarClasses` tests (Go + JS) pin the
  desktop-detach classes.

## Sky strip sync chip — drift wired (C-SYNC-DATE-BEACON, 2026-07-17)

The collapsible SKY strip's Calendaria sync chip (`calendar_v2_sky_strip.templ` +
`calendar_v2_shell.js` `wireSkyStrip`, C-CAL-SKY-STRIP/#545) shipped with a
fully-built, unit-tested DRIFT state that was dormant — no source persisted
what date Foundry last saw. C-SYNC-DATE-BEACON closes that gap on the
syncapi side (full design: `internal/plugins/syncapi/.ai.md` §"Notable
Design Decisions" — served-date beacon, throttle, Bearer-only auth gate,
member-read endpoint; single source of truth per T-O4, not duplicated here).
The calendar-plugin-owned half of the wiring:

- `skyStripCurrentDateString` (`calendar_v2_sky_strip_helpers.go`) formats
  `data.ActiveCalendar`'s **CURRENT** date (`cal.CurrentYear/Month/Day` —
  the real-time-seamed fields, same source syncapi's `GetCurrentDate`
  serves) as `"YYYY-MM-DD"`, SSR'd into the strip's `data-cal-current-date`
  attribute. Deliberately NOT `data.Year/Month/Day` (the navigated view
  date, which changes as the user pages the grid) — using the view date
  would compare the beacon against whatever month the user happens to be
  looking at instead of "today".
- `wireSkyStrip` fetches `GET /campaigns/:id/calendar-sync-beacon`
  (syncapi, new) alongside the existing `GET /campaigns/:id/foundry-presence`
  (foundry_vtt) — both plain client-side HTTP fetches, no new cross-plugin
  Go import (T-B2 unchanged). The beacon is gated by its OWN freshness
  (`SKY_STRIP_STALE_AFTER_MS`, the same 15-minute bar already used for
  presence staleness) before being fed to `computeSyncChipState`'s
  `fmConfirmedDate` param — an old beacon can't be trusted to represent
  what Foundry currently sees, so it's treated the same as "no confirmed
  date" rather than risking a false drift/false in-sync read. The beacon
  fetch degrades independently: its own failure doesn't sink the whole chip
  when presence data is fine.
- `computeSyncChipState` itself (the pure function, #545) is UNCHANGED —
  only its callers now populate real values instead of `''`.
- Tests: `calendar_v2_sky_strip_test.go` pins the `data-cal-current-date`
  attribute (including a regression for the view-date-vs-current-date mixup
  above); `test/js/calendar_v2_sky_strip.test.mjs`'s `wireSkyStrip`
  integration block drives the real fetch-and-paint chain end-to-end
  (drift / in-sync / stale-beacon-ignored / no-beacon / either-fetch-fails).

## Embed event chips now navigate to V2 (C-CAL-EMBED-CHIPS-TAP, 2026-07-18)

Wave-8 gate finding (#549 PASS_WITH_NOTES): the dashboard/entity-page
calendar embed (`CalendarEmbedFragment` → `calendarGrid` → `dayCell`,
`calendar.templ`) rendered event chips as `<button>` with `cursor-pointer`
+ a hover ring + full `data-event-*` attributes, but no click handler
existed anywhere in the embed context — the V1 inline scripts that once
wired them were correctly deleted by C-CAL-V1-SUNSET (#549), leaving a
silently inert affordance (pre-#549 it threw `ReferenceError` on click).

**Chose Option A (tap → navigate)** over Option B (demote to
non-interactive spans): the embed lives on dashboards/entity pages, so
leaving the page for the V2 calendar shell on tap is the honest
affordance, and it costs zero new routes.

- Chips are now real `<a href>` links (`dayCellEventHref`,
  `calendar.templ`) to `/campaigns/:id/calendar/v2/:calId?year=Y&month=M&day=D`
  — the SAME calendar the embed is bound to (`EmbedCalendar` accepts a
  `calId`/`calendarId`, so the embed isn't always showing the campaign's
  active calendar), cursored to the day via the `year`/`month`/`day` query
  params `ShowV2` already parses (`handler_v2.go`). No new route; the
  `internal/wire` route-snapshot test stays byte-identical.
- The href uses the day **cell's** year/month/day (`data.Year`,
  `data.MonthIndex`, the cell's `day`) — deliberately NOT
  `evt.Year`/`evt.Month`/`evt.Day`. For a recurring event those fields hold
  the original occurrence's creation date, not the date the chip is
  actually rendered under in the currently-displayed month; using the
  cell's date lands the tap on the occurrence the user was looking at.
- `data-event-*` attributes are left in place (harmless, and a plausible
  seam for the "read-only popovers in the embed" future flagged as
  out-of-scope for this dispatch) — only the interactive-affordance
  mismatch was in scope.
- `calendarGrid`/`dayCell` have exactly one caller (`CalendarEmbedFragment`
  in `blocks.templ`), so the blast radius is the embed only — V2 surfaces
  and `event_grid.js` (which only binds inside the V2 grid, matched via
  `[data-event-card]`, an attribute the embed chip never had) are
  untouched.
- Tests: `embed_chip_nav_test.go` pins the chosen affordance — no
  `<button>` in the rendered chip, and the exact `<a href>` (HTML-escaped
  query string) landing on the cell's date rather than a recurring event's
  stored date.
### Sync chip: "saw" → "applied" (C-SYNC-APPLIED-BEACON, 2026-07-18)

Upgrades the beacon above: a served-date GET proves Foundry fetched a date,
never that it applied one. Chronicle-side design lives in
`internal/plugins/syncapi/.ai.md` §"Notable Design Decisions" (new
`POST /calendar/date/confirm` endpoint, the same row extended with
`applied_*` columns); the calendar-plugin-owned half:

- `computeSyncChipState` (`calendar_v2_shell.js`) gains an OPTIONAL 7th
  param, `fmAppliedDate`. Internally `confirmedDate = fmAppliedDate ||
  fmConfirmedDate` — a fresh applied date wins over the served one for
  BOTH the drift comparison and the in-sync detail. Omitting the argument
  entirely (every caller/test that predates this change) makes
  `confirmedDate` collapse to exactly `fmConfirmedDate`, so the function's
  behavior is byte-identical to pre-C-SYNC-APPLIED-BEACON when nothing
  supplies an applied date — this IS the graceful-fallback pin for an
  older Foundry module build that never calls confirm.
- `wireSkyStrip` derives `fmAppliedDate` from the SAME
  `/calendar-sync-beacon` response used for the served date (no second
  fetch — the beacon response now carries `last_applied_date`/
  `last_applied_at` alongside `last_served_date`/`last_served_at`), gated
  by the identical `SKY_STRIP_STALE_AFTER_MS` freshness bar.
- Tests: `test/js/calendar_v2_sky_strip.test.mjs` gained 5 pure-function
  cases (omitted/empty arg falls back to served; fresh applied overrides a
  differing served date both toward in-sync and toward drift; stale
  applied is ignored) + 4 `wireSkyStrip` integration cases (same
  scenarios end-to-end, plus the explicit no-applied-fields-in-response
  graceful-fallback pin).

## Entity-tie visibility leak fix (C-CAL-ENTITY-TIES-LEAK-FIX, 2026-07-24)

`GET /campaigns/:id/calendars/:calId/events/:eid/entities` (RolePlayer-gated,
`ListEventEntitiesAPI`) returned every tied entity's name/type/icon/color with
**no entity-visibility filtering**: `EntitiesForEvent`/`EntitiesForEra`
(`entity_ties_repository.go`) took only an ID, never a viewer role/userID, so
no WHERE-clause gating was structurally possible. `EntitiesForCalendar` (the
Calendars-dashboard associations panel, C-APPS-CAL-DASH-W1) had already been
hardened with `entityVisibilityFilter(role, userID)` — cordinator#32 gap #1 —
but the initial audit missed the event/era-tie sibling methods, so a Player
could read a dm_only / custom-restricted entity's NAME via any event or era it
was tied to.

Fix: both methods now take `role, userID` and apply the SAME
`entityVisibilityFilter` EntitiesForCalendar uses (entities re-aliased `e` to
match the filter's hardcoded column prefix — the two prior queries aliased
`ent`). Threaded through `CalendarRepository`/`CalendarService` +
`entity_ties_service.go`; `ListEventEntitiesAPI` sources the viewer context via
`cc.VisibilityRole()` (co-DM/`IsDmGranted` counted as Owner, matching every
other calendar read path) + `auth.GetUserID(c)`. `EntitiesForEra` has no
production HTTP route yet (only events are routed in `routes.go`), so the
signature change is forward-looking — correct whenever a caller arrives, no
behavior change today. The syncapi cross-plugin `CalendarService` test stub
(`calendar_api_handler_test.go`) needed the same signature update.

Test depth matches the precedent (`EntitiesForCalendar`'s own fix shipped with
no live-DB matrix either): `TestEntityVisibilityFilter` covers the shared SQL
fragment; `TestEntitiesForEvent_ServiceDelegates`/`TestEntitiesForEra_ServiceDelegates`
pin the service→repo plumbing; `TestListEventEntitiesAPI_RespectsEntityVisibility`
(Player/Owner/co-DM matrix, event) and `TestEntitiesForEra_VisibilityMatrix`
(Player/Owner, era) drive a mock repo that models the real
`role >= RoleOwner` threshold to prove the handler now forwards real viewer
identity end-to-end — no MariaDB in this sandbox for a literal query-level
repro (no docker daemon; flagged for `make test-int` on a live DB).

## Event RSVPs (C-CAL-RSVP-P1, migration 013, 2026-07-25)

Calendar events now carry FIRST-CLASS RSVPs, distinct from the sessions plugin's
session attendance. See **ADR-046** for the full rationale; this section is the
implementation map.

**Supersedes** the in-code ruling on the drawer's disabled "Collect RSVPs" toggle
(`calendar_v2.templ`) that said RSVPs required an event↔session link. An event is
not a session — most events (festivals, downtime, one-off scenes) never will be.

**Storage (migration 013, plugin chain).**
- `calendar_event_rsvps` — one row per (event, user), `UNIQUE(event_id, user_id)`
  so re-answering UPDATEs in place. No response history to leak; a member cannot
  inflate a count by answering twice.
- `calendar_event_rsvp_tokens` — per-ACTION single-use opaque tokens
  (`crypto/rand` hex, 7-day TTL). Pattern-mirror of `session_rsvp_tokens` in a
  DISTINCT table, the `slot_proposal_tokens` precedent.
- `calendar_events.collect_rsvps` — per-event opt-in, default 0. Read rides
  `eventCols`; the WRITE is deliberately OFF the shared `UpdateEvent` path
  (`RSVPRepository.SetCollectRSVPs`) so the drawer's lossless quick-save, which
  re-sends the whole stored event shape, can never clobber the flag.

**Files.** `rsvp_model.go` (types, ENUM constants, action→status mapping),
`rsvp_repository.go`, `rsvp_service.go` (gating, tokens, week resolution),
`rsvp_handler.go` (campaign API + public token flow + the cross-plugin
interfaces), `rsvp_email.go` (fan-out + standalone token pages). Deliberately a
SEPARATE service/repository pair — `CalendarService`/`CalendarRepository` are
wide interfaces mirrored by a hand-written mock and the syncapi stub, so widening
them collides with every concurrent calendar lane.

**Routes** (`RegisterRSVPRoutes`, same middleware stack as `cg`):
- `POST /campaigns/:id/calendars/:calId/events/:eid/rsvp` — Player+; writes only
  the caller's own row (user id from the session, never the body). Optional
  `action` (`out_week` / `suggest`) reaches the two richer asks without a second
  endpoint per action.
- `GET  …/events/:eid/rsvps` — Player+. Counts for everyone; the per-person
  `responders` array is omitted server-side for anyone below Owner/co-DM
  (`CanControlWorldState()`), mirroring the scheduler overlay's
  density-for-all / identity-for-the-DM split.
- `PUT  …/events/:eid/rsvp-collection` — Scribe+. ON is the INVITE moment.
- `GET|POST /calendar-rsvp/:token` — PUBLIC, root-mounted, distinct from
  sessions' `/rsvp/:token`. GET = pure-read confirm interstitial (mail-scanner
  defence) or the suggestion form; POST = apply. Both ride the global CSRF
  middleware, so GET mints the cookie the POST double-submits.

**Security invariants** (each has a named test):
- One visibility predicate. Every path reuses `canUserView` — a second, subtly
  different path is exactly how the entity-ties leak happened.
- Self-write only. `AvailabilityExceptionWriter` and the RSVP upsert always
  receive the id from the session or the redeemed token.
- Tokens are re-checked at REDEMPTION for membership AND visibility, so a link
  cannot outlive the access that justified it. Unknown / spent / expired all
  return one generic message so the route is not an existence oracle.
- Consumption is atomic (`used_at IS NULL` in the UPDATE); the loser of a
  concurrent double-submit writes nothing.
- "Suggest another time" writes a NOTE + an owner notification. It does NOT mint
  a slot proposal — proposal creation is Scribe+ by ruling.
- RSVP data never rides the campaign or AI export (`rsvp_egress_test.go`).

**UI.** The drawer (Scribe+) owns the collection toggle and the counts/responders
readout. The QUICK-EDIT CARD owns the answer buttons, because the drawer is
Scribe+ and players never receive its DOM — one block there covers month chips,
ledger rows, and mobile agenda cards, which all open it via `[data-event-card]`.
Wiring is `addEventListener` in `event_grid.js` with per-node `dataset.*Wired`
guards, matching the file's existing convention: the quick-edit and drawer
scaffolds are PAGE-level (rendered by `CalendarV2Page`, outside the HTMX-swapped
view slot), so the inline-IIFE-onclick rule for HTMX-loaded fragments does not
apply here.

**"Out this week" week resolution.** Availability exceptions are real-world
dates; a calendar event may be in fantasy reckoning. When the calendar
`UsesRealTime()`, Y/M/D IS the Gregorian date and the event's own Monday-anchored
week is used. Otherwise it falls back to the week containing the redemption
moment — the scheduler's own "out this week" meaning. The resolved week is ALWAYS
named back to the member, so the action can never silently block the wrong one.
Days that already carry any exception are skipped, mirroring
`static/js/availability.js` `fireOutWeek`.

**Cross-plugin seams** (rule 8 / ADR-042; all nil-safe, all wired from
`internal/app/routes.go`): `MailSender` (smtp), `RSVPNotifier` →
`sessions.NotifyUsers` (the first external writer of the generic notifications
store, T-B2), `AvailabilityExceptionWriter` → sessions `ListMyExceptions` /
`ReplaceMyDayExceptions`, `RSVPMemberDirectory` → campaigns. The calendar gains
no import edge into sessions — `internal/wire/plugin_import_guard_test.go`
forbids it. With no SMTP and no scheduler wired, in-app RSVP still works.

**Fixed in passing (pre-existing, blocking).** `scanEvents` supplied 36 Scan
destinations for `eventCols`' 37 columns — `&evt.RecurrenceDayOfWeek` was never
added when migration 011 introduced the column. Every event LIST query
(month/week/day/range/upcoming/search/ledger) therefore failed against MariaDB
with `sql: expected 37 destination arguments in Scan, not 36`; only the
single-row `GetEvent`, which has its own correct inline scan, worked. No test
could catch it (nothing in the repo executes real SQL).
`event_scan_contract_test.go` now parses `repository.go` and pins SELECT arity ==
Scan arity for both read paths.

## Temporary offered availability from RSVP (C-CAL-RSVP-P2, 2026-07-26)

Extends C-CAL-RSVP-P1's "Suggest another time" from a free-text note into
STRUCTURED temporary availability. See ADR-046 §"Amended" for the rationale.

**The gap it closes.** P1 shipped an asymmetry: "Out this week" wrote structured
*un*availability the scheduler could use, but "Suggest another time" wrote prose
the Director had to read and re-key. Players could say when they *couldn't* play
in a schedulable way, and only when they *could* in English.

**What it does.** The suggestion surfaces (quick-edit card + the emailed token
page) now take date + from + to rows alongside the optional note. Those windows
become real `availability_exceptions` rows with `state = 'available'`, so they
land in the DM's week overlay and count toward the computed best window. No new
tables — the storage already supported `available`/`preferred` per date; only a
safe write path was missing.

**THE TRAP, and where the fix lives.** Exception rows fully REPLACE the recurring
pattern for a date (`effectiveBlocks`, `sessions/availability_overlay.go`). A
naive write of just the offered window would therefore ERASE the member's usual
hours for that day — a player answering "I could also do Tuesday evening" would
come out LESS available than before. The composition lives in the SESSIONS
plugin (`sessions.AddMyAvailableWindows` → `composeOfferedDay`), not here,
because the replace-semantics is the scheduler's own invariant: it composes the
day (existing exceptions for the date if any, else the recurring pattern for
that weekday), paints the offer on a minute canvas, and writes the merged set.
Server-side, because this write arrives from an email link with no editor in
front of it. Pinned by `sessions/availability_offer_test.go`.

Painting rules: `preferred` is never downgraded by a generic offer;
`unavailable` IS overwritten (the member is explicitly saying they could make
it); unpainted minutes emit no row (on a date with exception rows, a gap already
means "not available").

**Division of labour changed.** `RSVPService.ApplyToken` no longer takes a note
and no longer writes one. It consumes the token and writes only the status the
action implies; `suggest` carries no status and returns early. Both richer
actions' side-effects are the handler's, because they need the POST body
(windows/note) or the scheduler — neither of which the service has an edge to.
`applySuggestion` is the single shared path for the in-app and emailed surfaces
so the two cannot drift.

**Degradation.** Availability writing is best-effort: the RSVP note and the owner
notification are the promise this flow makes, so a scheduler that is absent
(`h.availability == nil`) or erroring still records the answer and tells the
member their times weren't saved.

**Test-invariant amendment.** `TestEventQuickEditV2_PlayersReadOnly` used to
assert the player card contained no `<input>`/`<textarea>` at all. That was a
proxy for "a player cannot edit the event", and it outlived its meaning the
moment players legitimately type their own RSVP and their own offered times into
that card. It now pins the event-editing fields BY NAME (strictly narrower and
harder to bypass), and `TestEventQuickEditV2_RSVPSurfaceIsForEveryRole` asserts
the RSVP affordances render for non-Scribes — the gate split this feature
depends on.

## Phone Month|Agenda toggle — dead pill fixed (C-CAL-MOBILE-VIEWS-FIX, 2026-07-26)

**Step-0 verdict (the operator report was half right).** Reported symptom: on a
phone, the calendar shows Month|Agenda pills and then a full 7-column month
grid; the Agenda pill "does nothing" and the mobile layout "never swaps."
A headless probe of `main` at a true 390px viewport
(`calendar_v2_mobile_breakpoint_probe_test.go`) showed the breakpoint swap was
NEVER broken: `[data-month-desktop-grid]` computes `display:none` at 390px and
`mobileMonthAssembly` renders, with the reverse at 1200px. The "7-column grid"
in the screenshot IS the shipped mini-month navigator — the signed mockup
(`cordinator/mockups/renders/cal-mobile-390.png`) is itself a full-width
7-column mini-month, and on a month with no events the agenda below it collapses
to the one-line "No more events this month." empty state, leaving a bare grid on
screen. So there was nothing to fix in the swap.

**The pill, though, was genuinely dead (T-B3).** `mobileAgendaPill` was an `<a>`
whose `href` was `v2ViewHref(data, "month")` — the exact URL the page was already
on, while the always-selected "Month" pill sat beside it. Tapping it re-navigated
to a byte-identical render: no view change, no error, nothing. It also hardcoded
`aria-selected="false"` even though it was the active surface.

**Fix — a real two-state toggle on the existing month route.**

- `CalendarV2ViewData.MobileAgenda` is bound in `ShowV2` from `?agenda=1`
  (month view only). Presentation-only: no new route, no new query, both states
  render from the `data.Events` the Month view already loads.
- `mobileMonthAssembly` renders the mini-month navigator **only** when
  `!MobileAgenda`; the agenda state drops it so the list owns the viewport.
  `data-mobile-agenda-only` marks the state.
- The switcher restructured: **Month moved INTO** the `hidden md:contents`
  desktop group (Month/Week/Day/Timeline), and a new `contents md:hidden` phone
  pair (`mobilePhonePills`) carries Month|Agenda with real selection state.
  `v2MobileMonthHref(data, agenda)` builds the two URLs.
- Neither phone pill may render selected off the month view — a deep link to
  Week/Day/Timeline (unreachable from phone width, but linkable) must not paint
  a false "Month is active" highlight.

**Guards.** `calendar_v2_mobile_agenda_test.go` gained the dead-pill regression
pin (the two phone hrefs must differ; exactly one pill selected per state; the
agenda state must drop the navigator; no selection off month) plus a source pin
that `ShowV2` still binds the query param. The 390px browser probe
(`calendar_v2_mobile_breakpoint_probe_test.go`) is the end-to-end pin — see its
header for why a DOM assertion cannot cover this class, and note it SKIPS under
`-short` (CI's mode) and when Tailwind/Chromium are absent.

**Not done here.** The agenda's empty state is still a single line, which is what
made the shipped layout unrecognizable in the operator's screenshot. Making the
phone agenda span past the current month (or state the month it is showing) is a
UX change, not a restoration, and belongs to the v4 remodel arc.

## Entity-side tie reads: scan mismatch + calendar-level visibility gap (C-CALV4-TIEFIX-PB, 2026-07-26)

Two independent bugs sat under the v4 remodel's entity-page calendar Block,
found and fixed as a verify-then-fix carve-out ahead of the wider remodel.

**Bug 1 — `EventsForEntity` scanned 37 destinations against 39 selected
columns.** Its Scan list was missing `&evt.RecurrenceDayOfWeek` and
`&evt.CollectRSVPs` — the exact same class of landmine the C-CAL-RSVP-P1 fix
closed in `scanEvents` (see `## Event RSVPs` below), just never ported to this
file, which lives in `entity_ties_repository.go` rather than `repository.go`.
Confirmed by direct count (not assumed): `eventCols` is 38 columns
(`repository.go:909-918`), `EventsForEntity`'s SELECT adds
`l.participation_role` for 39, and its old Scan list supplied exactly 37
arguments. Every call failed at runtime with `sql: expected 39 destination
arguments in Scan, not 37`, and `entity_calendar_block.go:81` swallows that
error and degrades silently — so the entity-page calendar embed's tied-events
list has shown nothing since the day this file shipped. Fixed by adding the two
missing destinations in the same positions `GetEvent`/`scanEvents` already use.

**Guard widened, not duplicated:** `event_scan_contract_test.go`'s
`TestEventColsMatchScanDestinations` used to loop only `{GetEvent, scanEvents}`
(both in `repository.go`). `scanArity` now takes a filename parameter, and a
new `eventScanSites` table adds `{entity_ties_repository.go, EventsForEntity,
+1}` (the `+1` accounts for the joined `participation_role`). Proven to fail
against the pre-fix code first (`git stash` the Scan-list fix, re-run, see the
exact "39 destination arguments... not 37" failure), then re-verified green
after restoring the fix — the same discipline the RSVP-P1 guard established.

**Bug 2 (§18-C) — CONFIRMED ALREADY CLOSED, not re-fixed.** The dispatch's
Step 0 asked us to confirm PR #565 (commit `6fe78e4`) actually closed the
entity-tie visibility leak before touching anything. Verified:
`entity_ties_handler.go:50` forwards `cc.VisibilityRole(), auth.GetUserID(c)`
into `EntitiesForEvent`, exactly as claimed (line number matched, not just the
existence of the call). `EntitiesForEra` was confirmed to have zero
non-test callers and no HTTP route (`grep -rn EntitiesForEra` outside
`_test.go` files turns up only the interface declarations + its own
implementation) — a real gap to build later, not a live leak. No code changed
for this half; see `## Entity-tie visibility leak fix` below for the original
fix.

**Bug 1 item 3 — calendar-level visibility was still unenforced on this
path.** `EventsForEntity`'s query joins only `entity_event_links` +
`calendar_events`, never `calendars` — so an event tied to an entity, sitting
on a `dm_only` *calendar*, was reachable through the entity-page embed even
though a player could never reach that calendar any other way (normal
calendar-view access is gated by `calendarVisibleTo` before any event query
runs; this path had no equivalent gate). `entity_calendar_block.go`'s own
Go-side filter (lines 78-91) only re-applies `canUserView` per EVENT, which
never sees the calendar's own visibility.

**Fix:** a new method, `EventsForEntityFiltered(ctx, entityID, role, userID)`
on `*calendarService` (`entity_ties_service.go`), composes two EXISTING
`CalendarRepository` methods — `EventsForEntity` + `GetByID` (calendars
fetched once per distinct `CalendarID` among the ties, not once per event) —
and filters in ONE pass using the same two predicates every other visibility
check in this package already uses: `calendarVisibleTo` (calendar-level) and
`canUserView` (event-level). It is deliberately:

- **A new method, not a signature change to `EventsForEntity`.**
  `entity_calendar_block.go` (owned by a different C-CALV4 slice — not in this
  dispatch's file list) still calls the old, unfiltered signature; changing it
  would collide with that slice.
- **Not added to the `CalendarRepository` or `CalendarService` interfaces.**
  Both live in files this dispatch does not own (`repository.go`, `service.go`),
  and COMMON's Bounds section explicitly forbids widening the ~60-method
  `CalendarRepository` — doing so would force updating the hand-written
  `mockCalendarRepo` in `service_test.go`, also unowned, risking a collision
  with two other in-flight C-CALV4 slices. Reachable via a type assertion to
  `*calendarService`, the SAME pattern `SetBindingCleaner` already established
  (`service.go:304-307`, "reached via a type assertion in routes.go so the
  CalendarService interface stays unchanged").
- **Not wired into any call site in this PR.** `entity_calendar_block.go:78-91`
  is the natural caller (it would replace that hand-rolled per-event-only
  filter loop entirely), but that file belongs to a different slice. Wiring it
  in — and, at that point, promoting this method onto the `CalendarService`
  interface so a `CalendarService`-typed caller can reach it without a type
  assertion — is follow-up work for whoever owns that file.
  **Update 2026-07-28 (C-CALV4-SEAM-P5 stage 13):** the interface promotion
  happened — `EventsForEntityFiltered` is on `CalendarService` now (its only
  planned consumer, the phase-B entity-page host, lives outside the package
  and could not type-assert the unexported `*calendarService`). The
  `entity_calendar_block.go` wiring itself is still open for phase B.

Tests in `entity_ties_test.go`: calendar-level dm_only hides an event for a
player but not an owner; event-level dm_only still applies alongside the new
check (additive, not a replacement); owner/system-context (`userID == ""`)
short-circuits with zero `GetByID` calls; an entity tied to several events in
the SAME calendar triggers exactly one `GetByID` call, not one per event.
## calendar-v4 Block spine (C-CALV4-SPINE-P2, 2026-07-26)

Everything server-side that produces a `calendar_block.BlockData`. All of it in
NEW files inside `package calendar`; the V2 page is untouched and stays live.

```
block_geometry.go     one leap-aware month producer  (+ _test.go)
block_projection.go   the per-viewer projection      (+ _test.go)
block_service.go      narrow interfaces + the spine  (+ _test.go)
block_repository.go   the batched read surface
```

### Why a SECOND geometry exists

`calendar_v2_helpers.go` reads raw `Months[i].Days` (`monthDays` :277,
`monthEraBands` :961, `monthWeekRowCount` :1053, `daysInRow` :1071). That is
leap-BLIND: a flagged real-time calendar draws February 2028 with **28** cells
while `Event.OccursOn` — which routes through `Calendar.MonthDays` — places a
recurring event on **Feb 29**. The event exists and has nowhere to render, and
nothing on main pinned it. `block_geometry.go` routes every day count through
`Calendar.MonthDays(idx, year)`.

The V2 helpers are FROZEN for the whole calendar-v4 wave — they are the densest
source-text-pin site in the repo and three of those pins PANIC rather than fail.
The duplication is deliberate and temporary; the V2 page is retired by a later
wave. **Do not unify them opportunistically.**

What IS shared, on purpose: the weekday COLUMN comes from the existing
`v2WeekdayIndexFor`, so the Block, the V2 grid and recurrence all place a day in
the same column.

### The three-counter divergence — documented, pinned, NOT fixed

Three day counters disagree in this package:

1. `Calendar.AbsoluteDay` — leap-aware (sums `YearLengthForYear`). Moon phase only.
2. `Calendar.constLenDayIndex` — fixed geometry (`year*YearLength()`). Weekday
   column + recurrence.
3. The legacy V1 path.

1 and 2 diverge by **exactly one day per elapsed leap year**, so a per-day moon
disc drifts against its own cell by that amount. Coordinator ruling COMMON §6.4
freezes `constLenDayIndex` for wave 1: making it leap-aware would shift the
weekday column of every calendar already in the operator's production database.
`TestBlockCounterDivergencePin` measures it (year 40 → 10 days) and pins today's
numbers. Reconciliation is its own dispatch and needs an operator gate.

### The count-oracle rule (the load-bearing one)

`ViewerContext.TiedCount` and `WholeCount` come from **ONE** viewer-filtered
pass. Computing one pre-filter and one post-filter turns the tie toggle into an
oracle whose difference is the number of hidden events. The signed mockup's own
`tiedCount` is that leak and is deliberately not ported.

Compounding trap: `filterEventsByUser` compacts IN PLACE (`events[:0]`,
`service.go` :2394), so it mutates the caller's backing array — a SECOND
filtering pass reads a corrupted slice.
`TestFilterEventsByUserMutatesTheCallerSlice` documents the hazard;
`projectBlock` filters once and nils its input.

### Performance: one absolute day per month

`Calendar.AbsoluteDay` loops `y = 0..year-1`, so one call costs
`year × len(Months)` iterations. Per cell that is `days ×` that. The producer
computes ONE base per month and steps integer offsets — the API takes
`baseAbsDay`, so a caller *cannot* reintroduce the loop.
`TestBlockMoonBaseDayIsSteppedNotRecomputed` asserts both exactness and the cost
ratio (measured 27.6ms → 0.91ms, ~30×).

The same shape at the query layer: `EagerLoadCampaignCalendars` costs **9**
queries for any number of calendars where `eagerLoad` (`service.go` :668) costs
9 **per calendar**. It deliberately does NOT load `Weather` — no `BlockData`
field consumes it — so `Calendar.Weather` is nil on everything it returns.

### Narrow interfaces, not a wider `CalendarRepository`

`CalendarRepository` is ~60 methods backed by a HAND-WRITTEN `mockCalendarRepo`
in `service_test.go`; widening it breaks the package test build for every
parallel slice at once. `BlockRepository` is the Block's own read surface,
constructed by `NewBlockRepository(db)` over the same handle, implemented by new
methods on the existing `*calendarRepo` in `block_repository.go`.

`service.go` gained exactly one additive method, `ApplyRealTime` — on
`*calendarService` only, NOT on the `CalendarService` interface, so the mock is
unchanged. The composition root reaches it by asserting to
`calendar.RealTimeSeam`.

`EntitiesForEventsBatch` is named with the `Batch` suffix on purpose: the
parallel C-CALV4-TIEFIX-PB slice owns `entity_ties_repository.go` and is adding
its own viewer-filtered variant there.

### `calendar.BlockSpine()` — the composition-root provider

`internal/app/routes.go` belongs to exactly ONE calendar-v4 slice for the whole
wave, and the surfaces that consume the spine (entity-page hosting, the Bench)
land afterwards. So `routes.go` calls `calendar.InstallBlockSpine(svc)` and
later slices read `calendar.BlockSpine()` — the same idiom
`systems.SetSyncMappingProvider` uses from the same file. It is an
`atomic.Pointer`, written once at boot; callers must nil-check.

### The wave-1 rulings, and where they are enforced

| Ruling | Where |
|---|---|
| Fog has no backend → `DayCell.Fogged` always false | `buildMonthGeometry`, `TestBlockFoggedIsAlwaysFalse` |
| Composed audiences do not exist → `AudienceMark` only from `dm_only` / `visibility_rules` | `blockAudienceFor`, `TestBlockAudienceMarkUsesOnlyWhatExists` |
| Sync numerator is DEFINED, not queried → `Linked` = 1 when a module is connected | `CalendarLinkStatus`, `TestBlockCalendarLinkStatus` |
| `constLenDayIndex` unchanged | `model.go`, `TestBlockCounterDivergencePin` |

The sync pill's denominator **never drops**: every failure path keeps `Total`
and lowers only the state. State `bad` ("Incompatible structure") is never
emitted — Chronicle cannot see the module's own calendar structure; the module
detects that on its side.

### Cross-calendar ordering (pinned)

Two in-world calendars with unrelated epochs have no natural order — sorting by
`(year, month, day)` across Harptos year 1523 and a Dwarven deep-count year 218
is meaningless. `UpcomingAcrossCalendars` orders by **`DaysUntil` ascending**,
counted in the event's OWN calendar's days, then calendar `sort_order`, calendar
name, the date tuple, event name, event id — a total, collation-independent
order. `TestBlockUpcomingOrderingAcrossIncomparableCalendars` pins it.

It also does not repeat `UpcomingByCalendar`'s leak: that path filters base
visibility in SQL only and never calls `filterEventsByUser`, so a
`visibility_rules` event hands its name to a player there.

### Known contract gaps (raised to the coordinator, see `.ai/todo.md`)

Two of the original five were CLOSED by the r51 pin amendment
(C-CALV4-SEAM-P5, 2026-07-27): the four `int64` identity fields are now
`string` and carry the real UUIDs (`TestBlockIdentityIntFieldsAreZeroed` was
inverted into `TestBlockIdentityFieldsCarryRealIDs`), and `Mark.Tied` is the
per-mark tie flag. Still open:

- Multi-day event SPANS have no field in `BlockData`.
- `EraBand.Edge` is unreachable while eras are year-granular.
- The signed "Needs eras" fault has no expressible trigger; the fault mechanism
  ships for the three structural failures that ARE expressible.
- ~~`MonthGeometry.MoonsDeclared` unpopulated~~ — **closed by C-CALV4-SEAM-P5
  stage 15 (2026-07-28)**: `buildMonthGeometry` sets it from `len(cal.Moons)`
  (independent of `ShowMoons`/`MoonCap` — the capped per-cell discs cannot
  supply it) and the Nameplate renders the "3 of 4 moons" badge past the grid's
  ceiling. Seam-pinned through the real `BlockService.Block` moon hydration
  (`TestSeam_DeclaredMoonTotalReachesTheNameplate`).

## calendar-v4 seam close (C-CALV4-SEAM-P5, 2026-07-27/28)

Phase A's four PRs were each green in isolation and composed into a Block that
was visibly wrong five ways — the seam between this package's producer and
`internal/widgets/calendar_block`'s renderer had no test on either side. This
slice's durable deliverable is that test, and it lives HERE on purpose.

### The seam suite (`block_seam_test.go`) — and why it is producer-side

Projects real fixture data through `projectBlock`, renders it through
`calblock.Block`, and asserts on the HTML. The widget package is
plugin-agnostic by construction, so a widget-side test can only assert on a
`BlockData` the test author wrote — for any defect that lives in the
producer's CHOICE of input, such a test is green precisely while the product
is broken (that is how all five defects shipped). **The rule: assertions about
what the producer chose to emit belong in this package; widget-side tests may
only pin renderer behaviour on hand-written `BlockData`.** Every seam
assertion was written first and watched failing on the unfixed code.

### What changed in this package, by stage

- **Stage 1 (r51 pin):** `projectBlock` carries the REAL ids — the four
  identity fields are `string` now, `CalendarSlug` means the slug again.
  `TestBlockIdentityIntFieldsAreZeroed` inverted into
  `TestBlockIdentityFieldsCarryRealIDs`.
- **Stage 2 (ink, not membership):** the producer emits the WHOLE
  viewer-visible mark set in BOTH tie modes and sets `Mark.Tied` per mark;
  the `tieMode == "tied" && !evTied → continue` drop is gone and
  `blockDecorateCells` takes no tie mode at all — a mode parameter there
  could only reintroduce the defect. Measured payload cost on the densest
  fixture: 1 byte (the mode attribute).
- **Stage 4 (§3.3 dogear):** `blockAudienceFor` returned `Restricted: true`
  on BOTH branches, so every `dm_only` day drew the diamond and the gold
  notch rendered nowhere. `Restricted` is the DISCRIMINATOR: `dm_only` →
  false (dogear), `visibility_rules` restriction → true (diamond).
  `TestBlockAudienceMarkUsesOnlyWhatExists` had pinned only the labels — it
  now pins the discriminator too.
- **Stage 9 (§4 visibility on the spine):** `Block()` and `EventsForDay()`
  resolve through `requireVisibleCalendar` with ONE return site for missing
  and hidden alike — `apperror.NewNotFound("calendar not found")`, the exact
  shape `handler_v2.go` answers with, so a hidden calendar's existence is not
  leaked by a distinguishable error. Also fixed the false invariant on
  `TiedEventIDsForEntity`: the doc comment claimed its input was already
  viewer-filtered while the call site passed raw `candidateEvents`; `Block`
  now filters BEFORE the tie read (`projectBlock`'s own filter is idempotent
  over the already-filtered slice).
- **Stage 10 (§5 recurring dedupe):** `candidateEvents` reads the rendered
  month plus every intercalary month, and the recurring-candidate widening
  returns every recurring row regardless of month — N+1 months meant N+1
  copies, duplicate chips per occurrence day and an inflated foot total
  (counts deduped on event id, marks did not). Deduped at the SOURCE:
  `candidateEvents` unions on event id, so every consumer reads one set.
- **Stage 13 (fold-ins):** `UpcomingAcrossCalendars` computes the
  loop-invariant `AbsoluteDay` base once per calendar and hands it to
  `nextOccurrence` (the COMMON §7 trap the moon path was restructured to
  avoid); `EventsForEntityFiltered` joined the `CalendarService` interface
  (see the TIEFIX-PB section's update note above). The timeline
  `visibility_override` count fix lives in `internal/plugins/timeline`.
- **Stage 14:** retracted the stale pre-r51 "KNOWN CONTRACT GAP" comment that
  stage 2 had left stacked above `blockDecorateCells`'s current doc comment —
  it described the retracted membership behaviour and a per-mark tie flag
  that now exists.
- **Stage 15 (2026-07-28, the declared-moon total):** `buildMonthGeometry`
  populates `MonthGeometry.MoonsDeclared` from `len(cal.Moons)` — deliberately
  independent of `ShowMoons` and `MoonCap`, because the capped per-cell discs
  cannot state how many moons exist (the data.go ruling). The renderer half
  (the Nameplate's "3 of 4 moons" badge, rendered only past the widget's
  `moonCap` ceiling) is in the widget book.
  `TestSeam_DeclaredMoonTotalReachesTheNameplate` pins the last r51 acceptance
  line through the REAL `BlockService.Block` path — the fake repo's
  `MoonsForCalendars` hydration — so a producer that stops setting the field
  fails the seam, not just a hand-written fixture.

Stages 5–8 and 11–12 changed the widget package, the stylesheet and the lint
guard — see `internal/widgets/calendar_block/.ai.md` (dated entry) and
`tools/check-calendar-v4-lints.sh` (templ-conditional scanner fix + guard
B4's `data-cell`/`data-row` subjects).

## calendar-v4 Shelf + the Almanac register (C-CALV4-SHELF-P7, 2026-07-28)

W-E filled zone D. With it the Block's four zones are all real.

### The Almanac register — the producer half

`buildMonthGeometry` now emits `MonthGeometry.Almanac []AlmanacMoon` (pin r53,
`cordinator decisions/2026-07-28-calv4-shelf-pin-amendment.md`): one entry per
**declared** moon, in the calendar's own declaration order, each carrying its
period, its turns this month, its drift and its illumination for **every day of
the month**.

**IT IS UNCAPPED BY `MoonCap`, AND THAT IS SIGNED ([S5]).** `MoonCap` is a
host-passed parameter — the entity host passes 3 — and this is the one
sanctioned place it is deliberately non-authoritative for a zone. It governs the
GRID's discs; it does not govern the surface the grid's ceiling points at. L21
caps the month grid so *"the grid can never grow with the fiction"*, and that
ceiling is legitimate ONLY because *"the Almanac carries every declared body at
full width"*. Before r53 the fourth declared moon had no illumination data
anywhere in `BlockData` — `DayCell.Moons` is capped — so it existed in the
database, was counted by `MoonsDeclared`, and was drawn nowhere.

`AlmanacMoon.Drawn` is the ONE place the renderer learns which bodies the
ceiling excluded; re-deriving it by counting `DayCell.Moons` would put
`moonCap`'s arithmetic in two places.

Four properties the seam suite holds, because none is visible from either side
alone (`TestSeam_TheFourthDeclaredMoonReachesTheAlmanac`,
`TestSeam_TheAlmanacRegisterIgnoresMoonCap`):

1. four declared moons → four lanes, at every `MoonCap` value;
2. the GRID still honours `MoonCap` in the same render, so the two cannot agree
   by both being wrong;
3. every lane carries its WHOLE month — the register is never partially filled,
   because the Month lane, the Tonight readout and the Moons arithmetic are
   three views of one pass;
4. the nameplate badge's restored *"— all of them are in the Almanac"* tail is
   TRUE in that render.

### The arithmetic is auditable, so none of it is a constant

The signed Moons panel's own footnote is *"no date in the register was typed by
hand"*. The producer therefore computes drift and turns against the month's
**real** day count (`blockAlmanacRegister`), and the phase samples come from
`blockMoonPhaseAt` — `Moon.MoonPhase` widened to a fractional day, because the
signed turn test reads the phase at d−0.5 and d+0.5. It mirrors the model's
formula rather than reimplementing it, and
`TestBlockAlmanac_FractionalPhaseAgreesWithTheModel` holds the two together on
whole days: if they ever diverge, the Almanac and the grid would draw two
different moons on the same night.

The register steps off the SAME base absolute day the discs step off
(`monthBaseAbsoluteDay`), so the fourth body is computed by identical arithmetic
to the three the grid drew.

### It is skipped when no Shelf is reachable

`blockMonthGeometryInput.ShelfHidden` mirrors `ShelfStub.Hidden`. The register
is the Shelf's data and nothing else reads it, so a Block whose host removed the
zone builds none — on a four-Block Bench that is registers not built. The LAYER
half of "reachable" is not knowable here (the producer emits DEF and the host
overrides `Layers` afterwards), so that gate stays in `block.templ`.

### Both zone chips are gone, and the pin that counts them is an enumeration

`block_projection.go` now sets **both** `NeedsBackend` flags false. The fields
stay: they are the HOST's honesty switch for a zone a future host docks but
cannot fill, not a wave marker. No new zone flag was minted — `legend`,
`horizon` and `moongraph` keep their unconditional chips inside their layer
gates, per the 2026-07-28 DEF/zone-chip ruling §2, because no filling wave is
named for them.

`bench_test.go`'s chip pin is re-stated as an ENUMERATION ([S12]): every
surviving chip named by file and line, the count following from the list. Four
Bench-side (`bench.go:840` session · `:867` sync · `:945` horizon ·
`bench.templ:257` RSVP header) and one Block-side, subtracted — the Shelf's
FILTERS panel, which is not a zone chip.

### The count oracle grew a seventh reading

`block_count_oracle_test.go` now scopes its Ledger row count to
`data-zone="ledger"` and adds the Shelf's: the Upcoming panel's rows come from
the same viewer-filtered pass as the Ledger head and the day-cell total, for all
three fixture viewers (GM 14 · Nissa 11 · Bryn 9). The scoping is not a
weakening — both zones emit the SAME row primitive on purpose, so an unscoped
count could no longer say which zone it counted.

## C-CALV4-LAYERS-P9 changes in this plugin (2026-07-29, wave 3 / W-F)

The slice that made three waves' worth of "no preference store exists" false.

**The store.** `internal/plugins/calendar/migrations/014_block_layer_prefs.*`
adds `block_layers VARCHAR(255) NULL` to `calendar_active` — extending the row
migration 007 already chose to extend, under PR #368 stop-and-flag #3, rather
than minting a table. Grain: `(user_id, campaign_id)`. `repository.go` gains
`GetBlockLayers` / `SetBlockLayers` beside the sidebar-pin pair, using the same
upsert with the same empty-`calendar_id` first-write fallback.

**`NULL` is not the empty set, and the distinction survives every layer.** The
repository returns `nil` for NULL and an empty NON-NIL slice for `''`; the
service preserves it; `resolveBlockLayers` (`block_layers.go`) branches on it.
`nil` renders the HOST'S SEED, `[]` renders a bare month. Collapse them and the
bare month is unreachable, which turns the default into a floor.

**Validation is asymmetric on purpose.** `SetBlockLayers` REJECTS an unknown key
(the whole write fails) because a silently dropped key half-applies a choice the
viewer cannot audit. `GetBlockLayers` FILTERS an unknown key with a `slog.Warn`
because the only way one reaches a read is a registry that shrank after a write
— a deploy, not a caller — and bricking a viewer's calendar over the product's
own history is the wrong failure.

**The route.** `POST /campaigns/:id/calendar/prefs` → `Handler.BlockPrefsAPI`,
on the existing authed `cg` stack at `RolePlayer`, literal path, **720 → 721
routes, one addition, zero removals**. NOT under `/calendar/v2/…`: that
namespace belongs to the frozen V2 shell and the Block outlives it. It answers
**204 with `HX-Refresh: true` and no body at all** — no fragment, no OOB target,
no host descriptor accepted, `user_id` from the session only. The security
review is `block_prefs_route_test.go`, one assertion per claim.

**The three producers.** `blockDefaultLayers` / `entityBlockLayers` /
`benchBlockLayers` all carried the same byte-identical paragraph saying the
store was W-F's; all three now resolve through `resolveBlockLayers` and set
`HasSwitchboard` and `PersistURL` TOGETHER (r54's pinned invariant). The store
is read ONCE PER PAGE — `blockLayerPrefsFor` — because the Bench composes four
Blocks and the preference is per-(user, campaign).

**Neither host seed gained a key** ([LYR-7]), and `TestBlockLayersAreDefOnlyInWave1`
inverted deliberately rather than being deleted, following the precedent that
file set twice for the two `NeedsBackend` halves. **R2-1 later SHRANK the entity
seed** to three keys and re-pinned `TestEntityCalendarBlock_HostLayerSet` the
same way — inverted with a comment, never softened.

### The Bench's section map, after round 2 (C-CALV4-BENCH-R2 slice R2-1, 2026-07-30)

`benchSurface` is eight children in contract order, and **four of them are now
`<details>` disclosures**:

| child | collapses? | closed summary says |
|---|---|---|
| `.phead` | **never** — a page that hides its name is not compact, it is anonymous | — |
| `benchRibbonView` `.ribbon` | **yes**, key `ribbon` (WHOLE, not per tile) | the Today tile's eyebrow + headline, plus the attention count **for a GM only** |
| `.sechead` "The bench" | **never** — it labels the stack, it is not a section | — |
| `.stack` (BOTH Blocks) | **never** — the subject of the page may not be inside a disclosure | — |
| `benchRsvpView` `.rsvp` | **yes**, key `rsvp` | `Rsvp.Headline` when filled, `Rsvp.Note` when not — **no new producer field was needed**, which is the best evidence the inventory is right |
| `benchNextUpView` `.nextup` | **yes**, key `nextup` | breadth + the soonest row ("across 3 calendars · next: 14 Kythorn — Harvest Moot") |
| `benchRowGridSection` | **yes**, key `rows` — the `<details>` **wraps `#cal-dash-grid` from OUTSIDE** | "2 other calendars · 1 needs setup", the setup half GM-only |
| `benchCaption` | **never** — it is the page's honesty statement | — |

**Four things worth knowing before touching a disclosure:**

1. **The `rows` disclosure must stay OUTSIDE `#cal-dash-grid`.** The pre-existing
   owner sort control `hx-get`s that id and swaps it. Render the `<details>`
   inside and every sort destroys and recreates it — the section snaps shut on
   each sort and the store desynchronises from the DOM.
   `TestBenchDisclosure_RowsWrapsTheSwapTargetFromOutside` asserts ancestry, not
   ordering.
2. **The flip rides on the `<details>`, never on the `<summary>`.** `toggle`
   does not bubble, so the listener must be on the target; and a click handler on
   the summary fires *before* the state changes, which would invert every write.
   `hx-vals` is a static JSON literal — `hx-vals='js:…'` is dead under
   `htmx.config.allowEval = false` (`static/js/boot.js:168`).
3. **Every summary is computed from the VIEWER's payload.** A player's ribbon
   summary walks the three tiles they received, so it cannot name a GM tile even
   by accident; a player's rsvp summary comes from the same audience-shaped
   fields `benchRsvpBuild` produces, so no lane can leak into it. **No summary
   carries `.badge.need`** — a summary is a sentence, not a status surface, and
   the chip is not diluted.
4. **Closed-by-default is at EVERY width**, because one server-rendered `open`
   cannot vary by viewport (ADR-048 §13, met a second time). The mobile half of
   the complaint is answered in CSS instead: at ≤640px `.bsurf` is a flex column
   and three `order` values put `.phead`, the "The bench" `.sechead` and
   `.stack` first — the calendar above the ribbon, at the named cost of one
   reading-order divergence below 640px (WCAG 1.3.2).

The store is `calendar_active.bench_sections` (migration 016), holding the
**CLOSED** set so `NULL` (never chosen → all four closed) stays distinguishable
from `''` (closed nothing → all four open). It rides the EXISTING
`POST /campaigns/:id/calendar/prefs` as `section=<key>`: zero new routes,
`routes_snapshot.txt` byte-identical. That branch answers **204 with no
`HX-Refresh`** while the `layers=` branch keeps it — the asymmetry is
load-bearing and the handler comment says why.

**Two movements worth knowing about.** `entityCalBlockStub` embeds
`CalendarService` as a nil interface, so a method added to that interface
compiles fine and panics at CALL time rather than failing the build — worth
remembering the next time the interface grows. And the seam fixture needed a
declared event CATEGORY: without one there is no `Mark.AxisLabel`, the legend
renders nothing, and "the legend layer is off" becomes indistinguishable from
"this month has no types" — the unfalsifiable absence that suite exists to
refuse.
