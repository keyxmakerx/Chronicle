# internal/widgets/calendar_block — the calendar Block

**Wave:** calendar-v4 wave 1, phase A (W-A) · dispatch `C-CALV4-BLOCK-P1`
**Contract:** cordinator `mockups/calendar-v4.html` @ 37cdd6d (SIGNED, IMMUTABLE
r43) + `mockups/renders/v4-*`
**Canon:** cordinator `decisions/2026-07-26-calendar-v4-canon-amendments.md`
(A1–A8, guards B1–B4)

## What this is

One plugin-agnostic widget package that renders a whole calendar as **four
zones**: A Nameplate · B Instrument · C docked Ledger · D Shelf. It imports
nothing from `internal/plugins/**` and never will — exactly the shape
`internal/widgets/calendar_v2` proves. Nothing here can collide with the
calendar plugin because it does not know the calendar plugin exists.

**There is no drawer.** The Ledger IS the drawer, permanently docked. Clicking a
day repaints a panel that is already on screen. Canon D9's 480px slide-in drawer
clause was STRUCK for this reason (canon amendment A3); it is why the Block &
Bench direction won its judging round.

## The three load-bearing decisions

### 1. Sizing is CSS, and it has to be

`boot.js:163` sets `htmx.config.allowScriptTags = false`, so a `<script>` inside
an HTMX-swapped fragment **never executes**. A JS-sized Block silently renders at
the wrong density after any swap. That is not a preference — a JS implementation
is broken by construction on this platform.

So: **size class is a container query on the host wrapper; density is a container
query on each cell.** There is zero JavaScript in this package.

| Thing | Where | Rule |
|---|---|---|
| size class | `.cal-block-host` (`container-name: cal-block`) | full ≥900 · std ≥300 · mini ≥240 · else sub-mini |
| density | each `.cell` (`container-name: cal-cell`) | measured column ≥84px → named chips, else underline |

`std` is the **unqueried baseline**, so a Block laid out before its container is
measured degrades to the middle class rather than to the widest.

`sizing.go` is the same arithmetic in Go. **Nothing in the render path calls it**
— it exists so `sizing_test.go` can pin the contract's model (the divergence
pair: Harptos 10 columns at host 1040 → 68.2px → underline; Gregorian 7 columns
at the same host → 97.4px → named).

### 2. Ten-day weeks are native

`--week-len` drives every column count and is written per month. There is no
literal 7 in the grid CSS and `TestCSS_NoLiteralWeekLength` keeps it that way
(every `repeat()` must take `var(--week-len)`; every `grid-template-columns` must
be a subgrid, a `--week-len` repeat, or the full-tier body split).

The **five-column rule** — the strong ramp step after column 5 — is read from the
producer's `Weekday.Half` / `DayCell.Half` / `EraBand.Half` flags, never
re-derived from a week length.

**Era bands are PER WEEK ROW.** An era spanning days 1–17 cannot be one band
across three rows: `span 17` silently wrecks the subgrid (design-notes §9
deviation 2).

### 3. Motion is a BUDGET, and it is exactly four items (W-B, 2026-07-28)

Wave 1 forbade motion outright and named W-B as the slice that re-opens the
question. It did, and what it opened is a budget rather than a permission.
`TestCSS_NoMotionAtAll` is now an **allowlist** — it was rewritten, not deleted,
and it still fails on everything outside the budget:

1. `background-color` and `color`, on the surfaces that ANSWER each other.
   Canon A5: ANSWERED "changes background-colour and ink hue ONLY. It never
   gains or loses shadow, never changes border thickness, never moves."
2. `transform: scaleX(var(--m-swell))` on a Ledger row's `.rail` — and **never
   on `.gr`**, the gold GM rail (cv4:435). A permission marker that animates
   reads as a state change. It is a transform rather than a width because
   `width:4px` on a flex item is a LAYOUT property (design notes §10 defect 3).
3. `m-latch` on the selected cell's ring: centre → corners, nothing slides.
4. **M5 non-target silence**, as a selector rather than a JS budget: rail and
   row transitions are killed unless the Block is `:hover` / `:focus-within` /
   `[data-swapping]`. **Adding Blocks must add RESTING cost only.**

All of it inside `@media (prefers-reduced-motion: no-preference)`. Under reduced
motion the colour change is instantaneous and every law still holds — which is
the proof that ANSWERED is a colour STATE and not an animation.

REFUSED, not omitted: `will-change` · `@starting-style` · `view-transition` ·
animating `box-shadow` apart from its ring (canon D3) · the `drawOnce()`
`--m-step` settle ladder, which is a JS post-repaint pass and there is no JS
here. **The month grid never moves** (2026-07-27 motion policy): the Ledger's
row filter is the ONE sanctioned content change and it is bounded to `.lrows`.

Two rules survived W-B and must survive everything after it:

- **canon guard B1** — `--dash` / `--gap` are NEVER animated. Morphing a dash
  pattern destroys the greyscale identity channel the signed greyscale render
  promises, and it is the obvious "improvement" the next hand reaches for.
- **canon D3** — the hover ring is a 0.5px spread carried INSIDE `box-shadow`, so
  a shadow animating alone is structurally impossible.

## Files

| File | What |
|---|---|
| `data.go` | **PINNED — but the pin is a SHAPE pin, not a byte pin, and has not been byte-identical since 2026-08-08.** It was byte-identical to `cordinator dispatches/chronicle/C-CALV4-BLOCKDATA.go.txt` until C-CALV4-SKY (R2-5) added `SkyGradient` and `SkyClock` under [SKY-6], **authorised by name and by exactly two fields**. What enforces it now is `TestBlockData_ShapePinned` (`data_test.go`), a reflect field-order-and-kind pin. Editing the struct is still a STOP-AND-FLAG: it desynchronises parallel slices, and the whole value of the pin is that a third field cannot be added quietly. See "The sky header lives in two packages" below. |
| `helpers.go` | validation at the boundary + the honesty guarantees; since R2-5 also the sky's producer-free helpers (`skySeated`, `skyStyle`, `skyHeadLine`, `skySubHead`, …) |
| `sizing.go` | the sizing contract in Go, for tests only |
| `widget.go` | the public surface + the registries the tests pin |
| `block.templ` | the four zones, the stylesheet link, the below-month layer zones, and **`skyHeader`** — the sky's markup, gated on `skySeated(d)` (R2-5) |
| `nameplate.templ` | zone A |
| `sync_pill.templ` | the five honesty states, both strings |
| `instrument.templ` | zone B — the grid, the bands, the cells, the intercalary row, the sub-mini tick rule |
| `marks.templ` | chips, underlines, moon discs |
| `ledger.templ` / `shelf.templ` / `almanac.templ` | zones C and D — both FILLED (W-B, W-E). The stub names are gone; the `data-zone` markers are not, because the seam suite keys on them |
| `switchboard.templ` | **W-F.** The layer sheet: a top-layer `[popover]`, sibling of `.block`, opened declaratively by `popovertarget`. Two groups, eight rows, one footer. Every row is an `hx-post` carrying the RESULTING SET |
| `legend.templ` / `moongraph.templ` | the two below-month zones W-F FILLED. `legend` from `Mark.AxisLabel` (r52, type axis only); `moongraph` from `MonthGeometry.Almanac` (r53) with **no new pin field**. Both render nothing at all when they have nothing to draw |
| `static/css/calendar-block.css` | unlayered, self-contained, exempt (see below) |

## The stylesheet

**Unlayered and self-contained**, per cordinator
`decisions/2026-06-05-rendering-canvas-css-exemption.md`. `input.css`'s content
is layered; an unlayered standalone sheet beats every layer, which is exactly why
the two cascade regimes never fight. It carries an EXEMPTION MARKER so the
Wave-6 customization-readiness sweep skips it knowingly.

**Every selector is scoped under `.cal-block-host`.** An unlayered sheet outranks
the app's layered CSS, so a bare `.badge` rule in it would silently restyle the
whole product. `TestCSS_EverySelectorIsScoped` parses the sheet and enforces it.

The ONE token inherited from the host page is `--color-accent`, because the
campaign accent IS chrome. It never touches event data.

Linked only via `layouts.AssetURL` — a bare `href="/static/…"` in any `.templ`
fails `TestTemplatesUseAssetURL` repo-wide. The `<link>` is emitted *inside*
`Block()` so an HTMX-swapped fragment carries its own styling; a duplicate link
to an identical URL costs one deduplicated request.

## The sky header lives in TWO packages, and the motion budget above is still four (C-CALV4-SKY / R2-5, 2026-08-08)

R2-5 seated a sky header on the Block. **Its markup is emitted here; almost none
of its CSS is.** That split is deliberate and it is the thing to know before
touching either half, because reading only one of them teaches a wrong law.

**Measured 2026-08-08:**

| | `static/css/calendar-block.css` (this package's sheet) | `static/css/calendar-bench.css` (the calendar plugin's) |
|---|---|---|
| selector lines naming a sky class | **5** — `.skyfoot`, `.skyturn` ×3, `.alane span.c i.skyturn` | **65**, every one of them `.cal-bench .cal-block-host .skygrow …` |
| `.skygrow` rules | **0** | all of them |
| lines mentioning `sky` at all (incl. comments) | 10 | 122 |

**So: the sky's STATIC presentation is this package's, scoped `.cal-block-host`
like everything else here — the "every selector is scoped" law is unbroken and
`TestCSS_EverySelectorIsScoped` still proves it. Every TRANSITION the sky runs
lives in the plugin's Bench sheet**, double-scoped under `.cal-bench`, inside the
product's ONE motion register. R2-5 did not relax that register; it bought a
**per-class exemption for `.skygrow`** with the forbidden-ancestor list left
byte-identical, and renamed the guard
`TestBenchCSS_TheNamedCarveOutsAreExactlyTwo` so its name stops claiming a
monopoly it no longer has.

**CONSEQUENCE — THE FOUR-ITEM MOTION BUDGET ABOVE IS UNCHANGED AND UNAMENDED.**
The sky animates seven properties. Zero of them are in this package's sheet, so
`TestCSS_NoMotionAtAll`'s allowlist did not grow and needed no edit. A reader who
watches markup *this package emits* animate on screen will reasonably conclude
the budget went from four to eleven. **It did not.** Count the budget against
`calendar-block.css`, never against what renders.

**THE FOOTGUN, and it is a live one for the next host.** A Block rendered
*outside* a `.cal-bench` root emits fully-formed sky markup that is statically
styled and **never animates** — no error, no fallback, no test failure. Nothing
in this package prevents that. It is unreachable today for one reason only: the
**producer** fills `SkyGradient` on the Bench's PRIMARY Block and nowhere else,
and `skySeated(d)` gates the whole header on that field being non-empty. That is
a gate in `internal/plugins/calendar`, in a different package, enforced by
nothing here. **Any future slice that seats a sky on a second host inherits a
silent motion loss** and must either move the register or take the same named
exemption. Seat one, and the fix belongs in that slice, not in a later bug.

`SkyGradient` is also why the seat gate reads as data rather than a flag: an
empty gradient means *"this surface carries no sky"*, never *"the sky failed to
load"*, on the Almanac tab's absence-is-the-honesty-mechanism precedent. And the
gradient crosses the seam **already composed** rather than as a 0..1 fraction
because this package imports nothing from `internal/plugins/**` by construction —
a fraction could only become a gradient by re-deriving one here, which [SKY-6]
forbids, and carrying both would be the third field. `data.go`'s own comment
states this at length; the type shape is booked for a coordinator ruling.

## Channel discipline (canon A7)

Three colour channels, and they never mix:

- `--axis` — the event axis. The marks layer is the ONLY layer that may read it,
  and it is **forbidden from referencing `--accent`**.
- `--cal` — calendar identity (the Nameplate dot, the mini rail).
- `--bandhue` — the era tint, driven by the **ERA** and never hardcoded per
  calendar (`calendar-v4.html:1566` hardcodes Harptos and is a known defect).

None of the three is `@property`-registered as animatable. That is a recorded
**REFUSAL**, not an omission: interpolating an inherited registered custom
property is a per-frame style recalculation across up to 900 gradient-backed
nodes. A later hand proposing it should be pointed here.

Colour is never load-bearing: eight locked (hue, pattern) pairs, `p1`–`p8`,
implemented as `--dash`/`--gap` repeating gradients. Every mark must still
resolve under `filter: grayscale(1)`.

## Honesty states

| State | Behaviour |
|---|---|
| `Fault` non-empty | the fault prints **where the date would go** and **no date element is emitted at all** — not a zero, not an em dash, not a placeholder |
| sync pill | both Full and Compact strings are always in the DOM; the container query chooses. **The denominator never drops** — `SyncFullText`/`SyncCompactText` repair a producer that loses it |
| Ledger | **FILLED since W-B (2026-07-28).** Docked when its layer key is enabled AND the host has not hidden it (`ledgerDocked()`, the one predicate `block.templ` and `instrument.templ` both read). Rows are reassembled from `Month.Rows[].Cells[]` + `Month.Intercalary[].Marks` — never a parallel list, which is what makes the head count, the foot count and the day-cell total three readings of ONE number. Carries **no `needs backend` chip at all**, whatever `LedgerStub.NeedsBackend` says: the zone has content and a chip beside it would be a lie. Its 300px IS the 300px the full-tier arithmetic subtracts |
| Shelf | **FILLED since W-E (2026-07-28).** Docked on `shelfDocked()` — the Shelf's half of `ledgerDocked()`, and the one predicate `block.templ` and the moons-badge tail both read. Three tabs for a GM, two for a player: Upcoming (the Ledger's row primitive VERBATIM, month-scoped, capped at four later rows and saying so), Filters (the TAB, not the engine — one `needs backend` chip, W-F named as the filling wave) and the Almanac. Carries **no zone chip at all**, whatever `ShelfStub.NeedsBackend` says. `Hidden` REMOVES the zone rather than collapsing it, or the Block's declared heights stop being invariant |
| Almanac | the celestial register, and L21's other half. Three sub-tabs — Tonight (keyed to the SERVER-RENDERED day, not the CSS selection), Month (one lane per moon × one column per day of the month), Moons (the printed arithmetic). It reads `MonthGeometry.Almanac`, which the producer fills **UNCAPPED by `MoonCap`** ([S5]) — the grid's three-moon ceiling is only legitimate because every declared body is here at full width. Absent, tab and all, when the moons layer is off or the calendar declares none |
| `legend` | **FILLED since W-F (2026-07-29).** One swatch per event TYPE with a viewer-filtered count, built from ONE pass over the same `Rows[].Cells[].Marks` the grid drew — so the grid, the docked Ledger and the legend cannot drift. It JOINS the count oracle: every per-type figure must be independently reproducible from that viewer's own visible set. A type whose every event is hidden from a viewer is ABSENT from their legend — **not even a zero** (r52 rule 3). TYPE AXIS ONLY: there is no `Mark.OwnerLabel` and no per-calendar label, so the picker's other two options are booked to C-CALV4-AXIS-P11 rather than stubbed. An empty legend renders no zone at all |
| `moongraph` | **FILLED since W-F (2026-07-29), with NO new pin field** — r53's `MonthGeometry.Almanac` closed the data dependency completely on its way past. One lane per DRAWN body: **no composite, at any density** (L19/L24 — three attempts at one disagreed about the same five nights). The ceiling stays declared once, in the Nameplate (L30); the foot's `+N more in the almanac` names a DESTINATION, not a ceiling, and the destination is real because the register is uncapped by `MoonCap`. A 0%-lit night still draws a 1px bar, because a zero-height bar reads as missing data rather than as darkness. Every `.sfcell` carries `data-day` (guard B4) for the slice that pays for the ladder |
| `horizon` | **still chipped, and its chip is now ROLE-GATED (W-F §8.4).** There is no queryable fog horizon on main and `DayCell.Fogged` is false for everybody. A player who enables the key gets NOTHING — no zone, no greyed panel, no note — because `needs backend` never renders to a player and `display:none` is not absence. The ROW stays in the switchboard: the denominator never varies by role. Data + the `Reveal through` write are C-CALV4-HORIZON-P12, with their own security review |
| `switchboard` | **LIVE since W-F (2026-07-29).** `LayerState.HasSwitchboard` is true wherever the producer resolved a persistence endpoint, and `HasSwitchboard == (PersistURL != "")` is pinned (r54). The sheet is a top-layer `[popover]` — `.block` sets `overflow:hidden` and the host is an inline-size container, so nothing positioned inside the Nameplate survives. Anchored to the INSTRUMENT and not to the ⋯: the ⋯ sits directly above the docked Ledger, and a screenshot proved that anchoring there covers the Ledger's own "Event list" row |
| permission | **ABSENCE.** A player receives no dm_only mark: no placeholder, no ghost, no notch hinting one exists |

## Deliberate divergences from the contract (all flagged in PR)

1. **The `isNamed()` row-height clause is RETIRED, not inherited.** The mockup's
   second clause (`instrument / rows >= 68`, with a hardcoded 470px at full tier
   against a measured 563px) is a known, booked, deliberately-unfixed defect
   (canon §D). The CSS implements the measured-column clause alone; vertical fit
   is settled by real layout. Correcting the constant to 563 was **refused** — it
   desynchronises every signed still. `TestIsNamed_RetiredRowHeightClause` pins
   the divergence so it can neither be reintroduced nor silently lost.
2. **`.band.half` is applied, plus a `.halfrule` ruler.** The mockup declares
   `.band.half` and never applies it, so the ramp step stops dead at the era
   band. Applying the class alone cannot draw the rule where it belongs (a band
   is one grid item spanning many columns), so the band row also carries a
   zero-width ruler item on the half column itself.
3. **The week gutter's width is a container query, not an inline `--gut`.** The
   mockup drops the gutter below 481px of host — a host measurement the producer
   cannot make. The mockup additionally gates it on `week === 10`; wave 1 does
   not, because a literal week length in the renderer is what this brief exists
   to remove.
4. **Out-of-range cells render empty.** `DayCell.Day == 0` IS the out-of-range
   marker, so there is no field for the adjacent month's ordinal; the signed
   Gregorian still shows a greyed 28/29/30/1. A `LeadLabel string` on `DayCell`
   would close it — that is a struct change and therefore a stop-and-flag.
5. **Not rendered, for want of a field:** the "colour by:" segment, the "N
   hidden" badge, the "3 of 4 moons" badge, the scope chip, and the real-world
   campaign clock. The viewer's ZONE *is* rendered, because `ViewerContext.Zone`
   exists. **Two of these closed since:** the hidden badge and the moons badge
   landed with r51/r52's fields, and `HasSwitchboard` landed with **r54** at
   C-CALV4-LAYERS-P9. The **colour-by segment did NOT**, and its blocker is now
   DATA rather than a preference field: there is no `Mark.OwnerLabel` (r52 §5
   refused it, CTS-5 SIGNED "omit") and no per-calendar label, so two of its
   three options cannot be built at all. W-F omitted it for the same reason
   waves 1 and 2 did — rendering it would fabricate a pressed state — and booked
   **C-CALV4-AXIS-P11**, which carries the pin amendment for both labels.

## Producer notes (read before writing a BlockData)

- **Do NOT trim `Marks` to a density's cap.** Density is a container query, so
  the producer cannot know which subtree will be visible. Hand over everything
  the viewer may see; each subtree applies its own ceiling. `MoreCount` is
  **OVERLAPPING**, not additive (r51 ruling, `data.go`): `Marks` holds the
  FULL viewer-visible list and `MoreCount` is how many of those are not drawn
  as chips — a day's total is `len(Marks)`, NEVER `len(Marks)+MoreCount`.

  **VERIFIED against the shipped producer — but the mirror is a SNAPSHOT.**
  C-CALV4-SPINE-P2 landed before this render tier, so the two halves were
  written by chats that could not see each other. `blockCapMarks`
  (`internal/plugins/calendar/block_projection.go`) keeps the full list and
  reports the overflow, and its cap constants reconcile with `chipsFor` /
  `moreCount` here. `TestMarkCap_AgreesWithTheProducer` pins the RENDERER's
  half of that seam on a hand-written input: its `producerSplit` is a local
  mirror keyed to this package's own `namedChipCap`, NOT to the producer's
  constant, so it CANNOT fail when a future producer starts trimming or the
  two caps drift — an earlier revision of this file claimed it could, and
  that one sentence is exactly the failure mode C-CALV4-SEAM-P5 existed to
  kill (a seam that "has a test" reads as covered). What the producer
  actually emits is pinned producer-side by the seam suite,
  `internal/plugins/calendar/block_seam_test.go`, which projects the real
  producer through this renderer and asserts on the HTML.
- `TiedCount` and `WholeCount` MUST come from **the same viewer-filtered pass**.
  One pre-filter and one post-filter turns the tie toggle into an oracle that
  differences hidden events out of the two numbers.
- `filterEventsByUser` mutates the caller's backing array (`events[:0]`,
  `service.go:2388`). Filter once, derive every count from that one pass.
- `Calendar.AbsoluteDay` loops `y = 0..year` — ~537k iterations per 30-day month
  at year 1492 if called per cell. Compute ONE base absolute day per month.
- `DayCell.Fogged` stays **false**: there is no queryable knowledge horizon on
  main. The `.cell.fog` CSS ships unused (wave-1 ruling 1).
- `AudienceMark` is populated ONLY from what exists: `visibility == dm_only`
  (`Restricted` false → the gold notch) or a `visibility_rules` restriction
  (`Restricted` true → the gold diamond). Composed tag+member audiences do not
  exist on main (wave-1 ruling 2).
- `SyncPill.Linked` is DEFINED, not queried: 1 when a module is connected, else
  0; `Total` is the number of calendars in the campaign (wave-1 ruling 3).

## Tests

```bash
go test ./internal/widgets/calendar_block/                 # everything
go test ./internal/widgets/calendar_block/ -run Probe -v   # the real browser
BLOCK_SCREENSHOTS=/tmp/shots go test ./internal/widgets/calendar_block/ -run Screenshots
```

| File | Proves |
|---|---|
| `sizing_test.go` | the divergence arithmetic, the size-class thresholds, and that the retired row-height clause stays retired |
| `block_test.go` | the render contract: data-day everywhere, fault-replaces-date, both sync strings, per-week-row bands, season-suffix-row-0-only, two cell subtrees, permission-is-absence, no inline motion |
| `css_contract_test.go` | the stylesheet itself: unlayered, scoped, no motion, no literal week length, the container queries, the D3 ring, the eight patterns, channel discipline |
| `container_query_probe_test.go` | a **real browser** measures the columns and picks the density. Skips under `-short` (CI's mode) and without Chromium — a skip is NOT a pass |
| `screenshot_gen_test.go` | the fidelity evidence, captioned with its own arithmetic |
| `sky_test.go` | the sky header's string contract — 11 tests. Includes the **two-directional** guard (`TestSky_ThePlaceholderIsGoneAndTheSkyArrived`): the retired `.skyband` class is ABSENT **and** the sky's own summary is PRESENT. A one-directional guard goes green on a slice that deleted the placeholder and shipped nothing in its place, which is exactly what a repo-wide grep proved nothing would have caught |
| `sky_measure_probe_test.go` | a **real browser** measures the three counts against the SHIPPED element: closed band 40 / 40 / 32px, disc growth 13→40px and 11→32px, anchored-edge travel |
| `sky_pane_probe_test.go` | the open pane's two shapes from the signed stills — the muted sub-head and the four-column Tonight row |

**CI runs `-short`, so the probe never executes there.** Container-query density
proven only by a Go unit test is not proven; run the probe locally and paste the
measurements.

## Measured vs modelled columns (probe, Chromium 141)

| host | week | class | measured | model | delta | density |
|---|---|---|---|---|---|---|
| 1040 | 10 | full | 68.4px | 68.2px | +0.2 | underline |
| 1040 | 7 | full | 101.0px | 97.4px | +3.6 | named |
| 1232 | 10 | full | 87.6px | 87.4px | +0.2 | named |
| 358 | 10 | std | 32.2px | 34.2px | −2.0 | underline |
| 280 | 10 | mini | 25.2px | 26.4px | −1.2 | underline |
| 220 | 10 | sub-mini | — | 20.4px | — | grid dropped |

The 7-column row's +3.6 is the model assuming a week gutter that the Gregorian
Block does not open. Every row lands on the same side of the 84px line under both
numbers; the probe asserts that explicitly.

## Wave map

W-A (this) → **W-B filled the Ledger and landed the ANSWER primitive (DONE,
2026-07-28)** · W-C hosts the Block on entity pages and wires the CSS-only tie
toggle (DONE) · W-D is the Bench (DONE) · **W-E filled the Shelf and landed r53's
celestial register (DONE, 2026-07-28)** — with it the Block's four zones are all
real and the calendar-v4 Block is architecturally COMPLETE · **W-F built the
layer system (DONE, 2026-07-29, `C-CALV4-LAYERS-P9`)**: the per-viewer store
(calendar migration 014 + one route), the live `⋯` switchboard, r54's
`PersistURL`, the `legend` and `moongraph` zones, and `SKY_ON()` restored in one
place. It deliberately did NOT take four things the earlier waves booked to it,
each now under a named follow-on — the **Filters engine**
(**C-CALV4-FILTERS-P10**, [S2]; the tab and chip W-E shipped stay), the
**colour-by picker** and the legend's other two axes (**C-CALV4-AXIS-P11**,
blocked on data), the **horizon data** and the `Reveal through` write
(**C-CALV4-HORIZON-P12**), and the **Tonight retarget** plus the
`.sp2`/`.almgrid` ladder extension (**C-CALV4-ANSWER-EXT**, in W-B's files). The
per-day overflow popovers (CTS-6) have no owner: P9 answered their blocking
argument and recommends re-booking or closing them.

**ROUND 2 (2026-08-08) touched this package exactly once, in R2-5
(`C-CALV4-SKY`)**: two `data.go` fields, `skyHeader` in `block.templ`, the sky
helpers, three new test files, and eight static rules in the sheet. **R2-4
(`C-CALV4-V2SUNSET`) and R2-3 (`C-CALV4-THEATER`) did not open a file here** —
R2-4 was a link sweep in the plugin, and R2-3 delivered no product code at all.
The Block's own contract is therefore unchanged by round 2 apart from the sky.

**One long-standing note in this file is now load-bearing elsewhere.** The Shelf
section below records that `AlmanacDay.Node` is false everywhere *because no
orbital-node data exists* — `calendar.Moon` has no node, no inclination and no
ascending-node column. R2-5's signed still asks the open pane for an *"Umber in
shadow, days 17-21"* window, which reads exactly that flag. It is **dropped and
asserted absent by name**, on the same reasoning that refuses a fabricated
sunrise. The absence recorded here as vocabulary is the reason a signed still
cannot be reproduced; see the divergence register in
`internal/plugins/calendar/.ai.md`.

## C-CALV4-SEAM-P5 changes in this package (2026-07-27/28)

The seam slice: phase A's producer and this renderer were each green in
isolation and composed into a Block that was visibly wrong. The durable fix —
the seam suite — lives in `internal/plugins/calendar/block_seam_test.go`
(producer-side, by rule: it asserts what the producer CHOSE to emit; tests
here may only pin renderer behaviour on hand-written `BlockData`). What
changed on this side of the seam, by stage:

- **Stage 1 (r51 pin):** `data.go` re-copied byte-identically from the amended
  pin — four identity fields `int64`→`string`, plus `Mark.Tied` and
  `MonthGeometry.MoonsDeclared`. The tie control gates on
  `HostEntity != ""` now; `data_test.go`'s reflect pins flipped to
  `reflect.String`; `intText64` deleted with its last call site (the `unused`
  lint has no `_test.go` exclusion).
- **Stage 2 (tie ink):** `marks.templ` stamps `tied`/`untied` per mark from
  `Mark.Tied`, and the CSS ink rule moved per-DAY → per-MARK, so "whole" mode
  dims individual untied chips. TieMode changes ink, never membership
  (`tie_ink_test.go`).
- **Stage 3 (identity triple):** `calToken` maps the closed token set
  `{harptos, real, elven, dwarven}` → `var(--cal-<token>)` (unknown token →
  structural fallback; the whitelist survives because a bare value reaching a
  `style` attribute is why it exists). `Pattern` renders as a ring on the
  Nameplate dot, `Letter` as a bordered mark (`identity_triple_test.go`).
  **The fixtures had been sending colour VALUES where the producer sends
  token names** — the widget suite was green BECAUSE the fixtures were wrong.
- **Stage 5 (overlapping MoreCount):** `helpers.go`'s `totalMarks` and
  `underlineRestAt` both read `MoreCount` as additive; both now use
  `len(c.Marks)` per the r51 ruling. The foot half is pinned by the seam
  suite; the underline half is unreachable from the seam (the producer only
  sets `MoreCount` where the two formulas agree) so
  `TestUnderlineRest_MoreCountIsOverlapping` pins it here on hand-written
  data — renderer behaviour, so this is the right side.
- **Stage 6 (`.band.half`):** the stylesheet's `border-right` on `.band.half`
  deleted — `Half` means the half column lands INSIDE the band, and the
  `.halfrule` ruler is the rule's single owner.
  `TestCSS_BandHalfDrawsNoRightEdge` keeps it from returning.
- **Stage 7 (chip gating):** the Ledger/Shelf stub chips sit behind their
  `NeedsBackend` flags (`stub_chip_test.go`). **Superseded for the Ledger at
  W-B (2026-07-28):** the zone is filled, so it carries no chip whatever its
  flag says — the flag did its job (the producer flipped it, no template was
  edited) and the renderer now refuses the chip outright, so a producer that was
  never updated cannot leave "needs backend" beside fourteen real rows. The
  Shelf half was retired the same way at W-E (2026-07-28), so BOTH zone chips
  are now gone and neither flag decides anything for its own zone — the flags
  stay because they are the HOST's honesty switch for a zone a future host docks
  but cannot fill. The one surviving Block-side chip is the Shelf's FILTERS
  PANEL, which is not a zone chip: it is the honest state of one tab inside a
  filled zone, and it renders to a GM only. The three W-F zone chips
  (`legend`/`horizon`/`moongraph`) stay unconditional ON PURPOSE — they have
  no governing field in the pin; inventing one is a pin amendment. Flagged
  upward.
- **Stage 8 (layer gating):** 3 of 8 `LayerState` keys gated nothing. The
  moon discs now answer the `"moons"` key (the `moongraph` key owns the
  illumination CURVE, not the discs), and each zone renders only when its key
  is enabled AND the host has not hidden it. **The fixtures were pinning the
  defect again** — they asserted docked zones while enabling neither zone
  key; they now match the signed stills' layer state. `TestLayers`' chip
  count was inverted 5→3, not deleted.
- **Stage 12 (guard B4 subjects):** `instrument.templ` stamps `data-cell` on
  dated cells (behind the same `c.Day > 0` gate as `data-day`) and
  `data-row` + a `-w`-namespaced answer key on week rows, so guard B4 finally
  has subjects (stage 11 fixed the guard's templ-conditional scanner FIRST —
  adopting the markers before that would have redded CI on correct markup).
- **Stage 15 (2026-07-28) closed the booked residual:** the Nameplate renders
  the "3 of 4 moons" badge (`moonsBadgeText`/`moonsBadgeTitle` in
  `helpers.go`) IFF `MonthGeometry.MoonsDeclared` exceeds the grid's `moonCap`
  ceiling (3) AND the moons layer is on (the signed `MOONS_ON()` gate — with
  the discs off, "3 of 4" claims a surface that is not there). Three or fewer
  states nothing extra, per the r51 acceptance line; the per-cell `Moons`
  cannot supply the number because they are already capped. No new CSS —
  `.badge.need` already styles it. `nameplate.templ`'s header now lists ONLY
  the "3 hidden" hidden-event-count badge as absent (its field still does not
  exist in the pin; still flagged upward). The producer half
  (`buildMonthGeometry` setting the field) and the seam pin live in
  `internal/plugins/calendar/`; `moons_badge_test.go` here pins both sides of
  the threshold on hand-written `BlockData` — renderer behaviour, the right
  side of the seam discipline.

**Recurring lesson, twice on this slice (§3.2 and stage 8):** when a test
passes and the product is broken, suspect the fixture before the assertion.

## C-CALV4-HOST-P3 changes in this package (2026-07-28)

Phase B gave the Block its first production caller (the entity-page embed,
`internal/plugins/calendar/entity_calendar_block.go`). One thing changed HERE:
the tie toggle stopped being inert.

**The toggle is a hidden radio pair plus `:has()`, and that is the whole
mechanism.** `tieCounts` (`nameplate.templ`) now emits two `input[type=radio]`
carrying `data-tie-pick="tied|whole"`, each addressed by a `<label class=tiebtn>`
stating its count; `calendar-block.css` reads `:checked` off the Block with
`:has()` and changes the untied ink level. Everything about why:

- **It cannot be JavaScript.** `boot.js:163` sets
  `htmx.config.allowScriptTags = false`, so a `<script>` inside an
  HTMX-swapped fragment never executes — a JS toggle would die after any
  binding swap, silently, in production only.
- **It cannot be a route.** Wave 1 freezes `routes_snapshot.txt`, and it does
  not need one: pin r51 already puts EVERY viewer-visible mark and BOTH counts
  in the DOM in both modes, so the flip has nothing to fetch. That is also why
  the pair can never be differenced to infer a hidden event.
- **`data-tie-mode` stays on `.block`.** It is the server's initial answer AND
  the whole story on an engine without `:has()`. The `:has()` rules outrank it
  on specificity alone (the `:has()` argument contributes its own), so the
  viewer's choice wins without depending on source order.
- **TWO ink levels, not one.** The signed `setTie()`
  (`mockups/calendar-v4.html:2823-2824`) inks an untied mark at **0.28** in
  tied mode and at `--m-untied-ink` = **0.70** in whole mode. This sheet had
  only the tied branch, using the WHOLE value for it: tied mode under-dimmed
  and whole mode did not dim at all. Now `--tied-ink` / `--untied-ink`, with
  `TestCSS_TieToggleFlipsInkWithoutJS` pinning that a rule actually READS the
  radios (an unread pair is a control that visibly does nothing) and that tied
  dims harder than whole.
- **The radio group name is a pure function of `(CalendarSlug, HostEntity)`**
  (`tieGroupName` / `tieInputID`, `helpers.go`). Not a counter and not random:
  two Blocks on one page must not share one piece of state, and the SAME Block
  re-rendered by an HTMX binding swap must keep the mode the viewer just chose.
  A pure function satisfies both and is identical across servers.
- `boolAttr` went with `aria-pressed` — the `unused` lint has no `_test.go`
  exclusion, so a helper without a caller reds the build.

**Pin refreshed, intent unchanged:** `block_test.go`'s `stripMode` strips
` checked` where it used to strip `aria-pressed`. The property it protects is
the same one — flipping the mode may change the mode attribute and the selected
state, and NOTHING else about the DOM.

**Measured, and it retracts a plausible-sounding assumption:**
`container-type: inline-size` does NOT make an element a containing block for
`position: fixed`/`absolute` descendants; `contain: layout` does. Chromium 141,
three identical hosts: plain `(0,0)`, `container-type` `(0,0)`, `contain: layout`
`(82,450)`. This matters here because §"Sizing is CSS" makes every host of this
Block a container, and the obvious worry — that containing a host traps a
modal rendered inside it — is not what browsers do. Do not re-derive it from the
spec sentence, which reads the other way.

## C-CALV4-LEDGER-P6 — the docked Ledger (2026-07-28, wave 2 / W-B)

Zone C stopped being a stub. The four things a later hand most needs to know:

### RULE — any new dated surface MUST carry `data-day`, or the Ledger stops answering it

This is the rule the whole ANSWER mechanism rests on, and forgetting it is
INVISIBLE in review: the surface renders correctly, and simply never lights.
REVIEW.md:282-286 caught the week view shipping columns with no key — *"the week
can never answer and the docked Ledger here never repaints"* — and it took 286
keys and commit `d8973f2` to fix after the fact.

Guard B4 (`tools/check-calendar-v4-lints.sh`) enforces it, but only for elements
that carry the `data-cell` / `data-row` marker beside it, so **a new dated
surface must take a marker too** or the guard cannot see it.

A dated surface also needs `data-day-ord` — the bare ordinal — if it is to
participate in the ladder, because the generated ladder lives in ONE static
sheet shared by every calendar and cannot know a slug. Two namespaces:
`dayOrdKey()` (`3`) and `intercalaryOrdKey()` (`i3`). A ladder that forgets the
second makes Midwinter stop answering, which is guard B4's failure mode one
level up.

### RULE — the Ledger is NEVER horizon-filtered

`visFor()` applies no fog filter, and the signed `v4-plus-list.png` shows the GM
Ledger listing day 26 *"Warden's writ due · GM"* while the grid blanks days
22-30. `layers-and-week.html:857` — *"the Ledger already carries the GM's working
list for fogged days, so the grid never has to"* — is the argument that let the
week-scale fog reversal be WITHDRAWN. `DayCell.Fogged` is permanently false
today so nothing bites; **W-F adds the GRID-side split only** and must not
"consistently" filter the Ledger too.

### The day pick is CSS, and its ceiling is a signed constant

CTS-1 signed Option A: no route, no JS, no snapshot churn. Selection is a hidden
radio group whose name is a pure function of the data (`dayPickGroupName`), one
radio per selectable day, each with a stretched `<label>` UNDER the cell's
interactive children — the dogear and `+n more` raise themselves above it,
because cv4:2775-2782 excludes them from day selection and a label wrapping the
cell could not express that.

The ceiling is `answerLadderDays = 40` + `answerLadderIntercalary = 8` (CTS-2),
mirrored into the generated ladder and pinned in both directions by
`TestCSS_AnswerLadderAgreesWithGo`. **Past the bound a day carries no control at
all** — no radio, no label, no dead affordance. The Ledger still lists it.

Regenerate the ladder with:

```
UPDATE_ANSWER_LADDER=1 go test ./internal/widgets/calendar_block/
```

144 rules, 26 702 B uncompressed / 1 738 B gzipped. The ANSWERED *look* is not
repeated 48 times: the ladder sets one inherited `--answer` custom property and
the hand-written §ANSWER rules interpolate every value between 0 and 1.

### `.lday` IS THE REVEAL TOKEN AND THE LEDGER ROW MUST NEVER CARRY IT

Two kinds of per-day surface live in Zone C and they are OPPOSITES:

| | at rest | when its day is chosen | selected by |
|---|---|---|---|
| `.lhead .lctx`, `.lzero.lday` | `display:none` | revealed | the class `.lday` |
| `.lrow` | visible | filtered OUT | the attribute `[data-lday]` |

The row was shipped carrying `class="lrow lday …"` while the ladder's reveal
rule was written as `.lday[data-lday="N"] { display: block }`. Choosing a day
therefore turned every listed row from the signed one-line flex row into a
stack, with the time string spilling past the row's fixed height onto the day
numeral below it. **It passed every test in the suite**, because `.lrow`'s
height is fixed (48px full / 44px std): the collapse changed no geometry, and
the guard that was meant to bound the ladder allowlisted the `display` PROPERTY
without looking at its VALUE.

Three locks now hold it, deliberately in three different files:

1. `ledgerRowClass` emits no `lday` (`helpers.go`), pinned by
   `TestLedger_RowCarriesNoRevealToken`.
2. the generated reveal rule NAMES its two surfaces —
   `.lhead .lctx[data-lday="N"]` and `.lzero.lday[data-lday="N"]` — and sets
   `display: revert`, never a box value, because the two surfaces have
   different natural boxes (a span in the head, a div in the rows) and a rule
   that has to know which is a rule that can be wrong.
3. `TestCSS_AnswerLadderChangesNothingButVisibility` checks the ladder's
   VALUES (`display` may only be `none` or `revert`) and its reveal SELECTORS.

And the probe now reads computed `display` and day/time overprint off a real
engine, because that is the only place a fixed-height collapse is visible.
**Any surface W-E adds to Zone C either is hidden at rest and takes `.lday`, or
is visible at rest and takes `[data-lday]` — never both.**

### The height invariance is MEASURED, not asserted

"Nothing reflows" is falsifiable and it is falsified in a real engine
(`TestProbe_LedgerHeightIsInvariantUnderSelection`, skipped under `-short`). Two
devices deliver it and both were found by that probe rather than by reasoning:

- at FULL tier the Ledger is **out of flow inside its own 300px column**. Both
  items of a grid row contribute to its height, so a docked Ledger listing
  fourteen 48px rows made the Block 780px unselected and 532px selected —
  choosing a day reflowed the whole component.
- `.lrows{min-height:176px}` applies **at full tier only**. The std Block
  affords the zone 140px, so a 176px floor written at every tier kept its
  declared size and landed 36px ON TOP OF THE SHELF. That is CTS-8's collision,
  answered inside this zone's geometry rather than by touching a host layer set
  (`TestProbe_StdTierFilledLedgerDoesNotCollide`, at both production host widths
  — entity 420px and Bench 358px).

## C-CALV4-SHELF-P7 changes in this package (2026-07-28)

W-E filled zone D. With it the Block's four zones are all real.

### The rule the next surface's author needs

**WHEN A PRIMITIVE IS SHARED ACROSS ZONES, EVERY RULE THAT NAMES IT MUST NAME
ITS ZONE AS WELL.** The Shelf's Upcoming panel reuses `ledgerRow` verbatim — on
purpose, so the two zones cannot drift and the count oracle can be an equality
rather than a hope — and the moment it did, two of W-B's mechanisms reached into
it:

| mechanism | what happened | the fix |
|---|---|---|
| the ladder's row FILTER (`.lrow:not([data-lday])`) | choosing a day hid the SHELF's rows too, and the Shelf's body is content-driven under its 132px ceiling, so the Block went 618px → 532px. The docked Ledger's own promise, broken by its own mechanism | the selector says `.lrows .lrow`, which its doc comment always claimed; `TestCSS_AnswerLadderChangesNothingButVisibility` now checks the FILTER selector as well as the reveal selector |
| the ladder's REVEAL rule (W-B's own fix round) | matched a class token the row also carried, and every listed row collapsed out of flex | the reveal names its two surfaces and the row dropped the token |

Both were unscoped selectors whose doc comments already claimed a scope the code
did not have. The guard must check the SELECTOR, not only the property.

The Zone C rule W-B wrote for this file still holds and now has a sibling: a
per-day surface either **is hidden at rest and takes `.lday`**, or **is visible
at rest and takes `[data-lday]`** — never both. Zone D adds: a surface that is
one of two zones' worth of the same component must be **reachable by a selector
that names its own container**.

### The CSS-only control budget, and the ceiling it hit

Three radio groups now — the tie toggle, the day pick, the Shelf's tabs — plus
the Almanac's sub-tabs, all hidden radios read by `:has()`, all named by pure
functions of the data so four Blocks on a Bench cannot fight over one state
while an HTMX swap keeps each viewer's choice.

**A server-rendered `checked` attribute cannot vary by container query.** That
is a real bound on the no-JS discipline and it is why the signed std
construction — four tabs in the Ledger head, the Ledger's rows as the `Month`
panel — is not buildable while selection is CSS-only: one group cannot default
to `Month` at std and `Almanac` at full. The Shelf therefore keeps its own strip
at every tier and `.ltabs` still carries `Month` alone. Booked in `.ai/todo.md`.

### The Almanac, in one paragraph

`MonthGeometry.Almanac` (r53) is a per-month celestial register: one entry per
DECLARED moon, in declaration order, produced **uncapped by `MoonCap`**. Every
number the three sub-tabs print comes from it, and every one is computed against
the month's REAL day count — the mockup's four thirty-day literals are fixed and
not ported, and `TestCSS_NoLiteralWeekLength` now covers the lane, which takes
its day columns from `grid-auto-flow` so no month length exists as a literal or
a variable `repeat()` count anywhere in the sheet. `AlmanacMoon.Drawn` is the
only place the renderer learns which bodies the grid's ceiling excluded.

There is **no epithet** (`calendar.Moon` has no such column) and **no node-window
bracket** (`.abr` ships as vocabulary the way `.cell.fog` does, and
`AlmanacDay.Node` is false everywhere because no orbital-node data exists).

### The std tier, measured

The Shelf is `flex: 0 1 auto` at std and `flex: none` at full. Filled, the zone
wants 166px inside a 520px Block that also holds a month and a docked Ledger,
and with `flex: none` the LEDGER absorbed the whole deficit — crushed below its
own head and tab strip, it put 10.8px of rows on top of the Shelf at both
production host widths. The Shelf is the zone that can give (its body already
scrolls; the Ledger's furniture can only overflow), so it yields. The signed
132px ceiling is unchanged at both tiers: a smaller std ceiling was measured and
made the result WORSE, because the Shelf absorbs the whole deficit either way.

The strip is `overflow-x: auto`: six controls do not fit 358px of Bench host,
and clipping the last sub-tab would leave a control nobody can reach and nothing
saying it is there.
