# Foundry VTT Module (`foundry-module/`)

## Purpose

The Chronicle Sync module for Foundry VTT enables bidirectional real-time sync
between a Chronicle instance and a Foundry VTT game world. It runs as a Foundry
v12/v13 module, connecting via REST API + WebSocket to the Chronicle backend.

## Architecture

```
module.mjs (entry point)
  └─ SyncManager (sync-manager.mjs)
       ├─ ChronicleAPI (api-client.mjs)   ← REST + WebSocket client
       ├─ JournalSync (journal-sync.mjs)  ← Entity ↔ JournalEntry
       ├─ MapSync (map-sync.mjs)          ← Map drawings/tokens ↔ Scene
       ├─ CalendarSync (calendar-sync.mjs)← Calendar events/date ↔ Calendaria/SimpleCalendar
       └─ ShopWidget (shop-widget.mjs)    ← Shop entity inventory in Foundry UI
```

### Data Flow

1. **GM starts Foundry** → `module.mjs` initializes SyncManager
2. **SyncManager.start()** → validates config, initializes modules, connects WS
3. **WebSocket connects** → performs initial sync (pulls mappings since `lastSyncTime`)
4. **Live updates** → WS messages routed to modules; REST for CRUD operations
5. **Bidirectional** → Foundry hooks detect local changes, push to Chronicle via REST

### Sync Guard

All sync modules use a `_syncing` boolean flag to prevent infinite loops:
- Set `true` before applying a remote change to Foundry
- Foundry's change hooks fire but see `_syncing === true` → skip pushing back
- Reset to `false` after the change is applied

## Files

| File | Purpose |
|------|---------|
| `module.json` | Module manifest. Foundry v12-v13 compatible. Optional deps: Calendaria, Monk's Enhanced Journal |
| `scripts/module.mjs` | Entry point. Registers settings on `init`, starts SyncManager on `ready` (GM only) |
| `scripts/settings.mjs` | 7 world-scoped settings: apiUrl, apiKey, campaignId, syncEnabled, feature toggles |
| `scripts/api-client.mjs` | REST client (Bearer auth) + WebSocket (auto-reconnect, message queue, event emitter) |
| `scripts/sync-manager.mjs` | Orchestrator. Owns API client, routes WS messages, manages sync mapping lookups |
| `scripts/journal-sync.mjs` | Entity ↔ JournalEntry bidirectional sync. Monk's Enhanced Journal support |
| `scripts/map-sync.mjs` | Map drawings/tokens/fog sync. Percentage↔pixel coordinate conversion. Scene-to-map linking via context menu |
| `scripts/calendar-sync.mjs` | Adapter pattern for Calendaria and SimpleCalendar. 0-indexed↔1-indexed conversion |
| `scripts/shop-widget.mjs` | Shop window (Application v1). Context menu, drag-to-character-sheet, real-time updates |
| `templates/shop-window.hbs` | Handlebars template for shop inventory display |
| `styles/chronicle-sync.css` | Status indicator + shop window styles |
| `lang/en.json` | English localization for all UI strings |
| `TESTING.md` | 116-item E2E testing checklist |

## Server-Side Interconnects

| Client Feature | Server Plugin | Endpoints Used |
|---------------|---------------|----------------|
| API auth | `syncapi` | Bearer token in all REST calls |
| WebSocket | `websocket` | `GET /ws?token=<key>` |
| Entity sync | `syncapi` → `entities` | `/api/v1/campaigns/:id/entities/*` |
| Map sync | `syncapi` → `maps` | `/api/v1/campaigns/:id/maps/:mapId/drawings/*`, `.../tokens/*` |
| Calendar sync | `syncapi` → `calendar` | `/api/v1/campaigns/:id/calendar/*` |
| Sync mappings | `syncapi` | `/api/v1/campaigns/:id/sync/mappings`, `/sync/lookup`, `/sync/pull` |
| Media upload | `syncapi` → `media` | `/api/v1/campaigns/:id/media/upload` |
| Shop inventory | `syncapi` → `entities` | `/api/v1/campaigns/:id/entities/:eid/relations` |

## WebSocket Message Types Handled

- `entity.created/updated/deleted` → JournalSync
- `drawing.created/updated/deleted` → MapSync
- `token.created/moved/updated/deleted` → MapSync
- `fog.updated` → MapSync (renders fog regions as overlay drawings)
- `calendar.event.created/updated/deleted` → CalendarSync
- `calendar.date.advanced` → CalendarSync
- `sync.status` → API client (connection state)

## Security

- API key stored as Foundry world setting (GM-only visible, masked as password input)
- REST uses `Authorization: Bearer <key>` header
- WebSocket uses `?token=<key>` query param (necessary for WS upgrade)
- Per-key rate limiting enforced server-side (60 req/min default)
- Campaign-scoped: key can only access its bound campaign
- Device fingerprint binding: optional one-time device lock

## Known Limitations

- **Fog of war**: One-way sync (Chronicle → Foundry). Fog regions rendered as
  semi-transparent polygon drawings on the Foundry scene. No Foundry → Chronicle
  push yet (Foundry's native fog is vision-based, Chronicle's is polygon-based)
- **SimpleCalendar events**: Limited — events managed as journal notes, no CRUD hooks
- **Shop icon field**: Always returns null (`shop-widget.mjs:146`)
- **Single scene**: Only the active Foundry scene syncs (no multi-scene)
- **GM only**: Full sync runs only for GM users; players get passive updates via Foundry
- **Single text page**: Only first text page synced; multi-page journals lose content
- **Binary permissions**: Only `is_private` mapped to ownership; granular permissions ignored
- **No ownership push**: Foundry ownership changes not pushed to Chronicle
- **No actor/character sync**: No character sheet sync between Foundry actors and Chronicle entities

## Planned Features (Phase F)

See `.ai/todo.md` Phase F for full sprint breakdown.

### F-1: Journal Sync Fidelity
- Split entity `entry_html` by `<h1>`/`<h2>` headings into multiple Foundry pages
- Concatenate all text pages back into single `entry` for Foundry→Chronicle
- Track page mapping via `chronicle-sync.pageMap` flag
- Detect ownership changes in `updateJournalEntry` hook, push to Chronicle

### F-2: Granular Permission Mapping
- Map Chronicle `visibility: 'custom'` + `entity_permissions` to per-user Foundry ownership
  - Chronicle `view` → Foundry `OBSERVER`, `edit` → `OWNER`
- New syncapi endpoints: `GET/PUT /entities/:eid/permissions`
- Reverse-map Foundry ownership changes to Chronicle entity permissions

### F-3: System Detection & Character Field Templates
- Read `game.system.id` on init, match against Chronicle campaign systems
- System ID mapping: `{ dnd5e: "dnd5e", pf2e: "pathfinder2e" }`
- Only enable actor sync when systems match
- Per-system `character_fields.json` defining expected Character entity fields

### F-4: Actor ↔ Entity Sync (new `actor-sync.mjs`)
- Foundry Actors (type: "character") ↔ Chronicle entities (type: "character")
- System-specific adapters: `dnd5e-adapter.mjs`, `pf2e-adapter.mjs`
- Each adapter: `toChronicleFields(actor)` / `fromChronicleFields(entity)`
- Hooks: `createActor`, `updateActor`, `deleteActor`
- WebSocket: `entity.updated` with character type triggers actor update
- Dashboard "Characters" tab

### F-5: NPC Viewer / Hall (website feature)
- Campaign route `/campaigns/:id/npcs` — gallery of revealed NPCs
- Foundry integration: ownership change on NPC journal → auto-reveal on Chronicle

### F-6: Armory / Inventory System
- Items with game-mechanic fields, character "Inventory" tab via relations
- Foundry sync: Actor inventory ↔ Chronicle inventory relations

### F-7: Shop / Marketplace Enhancement
- Transaction logging, currency tracking, stock management
- Foundry: purchase → update character inventory on both sides
